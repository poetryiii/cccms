# 待办

> 图例：`[x]` 已完成 · `[ ]` 待办 · `[~]` 明确不做（附原因） · `[?]` 待决策

---

## 0. 背景与目标

**原状**：数据权限由 Logic 层**显式调用** `DataScope::row()` / `field()` 注入，仅 `user` 一张表接通。新增业务模块必须记得手写一行，漏写即静默失效。

**目标**：把数据权限下沉到 `BaseModel` 全局查询作用域，做到「**默认生效、按表收窄、可显式逃生**」：

- 新插件只要用模型查询，就自动受数据权限管辖，不依赖开发者的记忆；
- 只有登记在 `sys_data_scope_table`（受控表）的表才能配**自定义规则**；预设基线由模型声明决定；
- 单条读写入口（详情/编辑/删除/重置密码）自动被覆盖，不再需要逐个手写 `assertInScope`。

**两个必须区分开的概念**：

| 概念 | 含义 | 决定方 |
|---|---|---|
| **模型化** | 该表查询改走 `Model` 而不是 `Db::name()` | 各模块迁移进度 |
| **参与数据权限** | 是否走预设基线（仅本人 / 本部门） | 模型的 `$dataScope` 声明 |
| **登记受控** | 是否可配**自定义**行级 / 字段级规则 | `sys_data_scope_table` |

---

## 1. 迁移总览

### 1.1 三条设计铁律

1. **表名以模型为准**：`applyToModelQuery()` 强制用 `$model->getName()` 覆盖声明里的表名，避免声明与实际不符。
2. **用户上下文只从 `request()->user` 取**，不新建静态容器。
   `Workerman\Coroutine\Context\Fiber` 在**非 fiber 环境**会把数据写进 `static::$nonFiberContext`（worker 级全局），webman worker 常驻不会自愈 → 会跨请求泄漏用户身份。用 `request()->user` 则随请求销毁。
3. **无用户 ⇒ 不作用域**（fail-open，但边界封闭）：只剩登录链路、CLI、`#[NoLogin]` 三类场景，且 `CheckLogin`/`CheckAuth` 会先对受保护路由抛 401。

### 1.2 表 → 语义 → 状态

> 依据 `db/schema.sql`：**全库仅 `sys_file` 有 `create_by`；没有任何表有 `dept_id` 自有列**（`dept_id` 只在 `sys_user_dept` / `sys_dept_role` 关联表里），所以每张表的 owner / dept 语义都必须显式声明。

| 功能模块 | 表 | 模型 | owner（仅本人） | dept（本部门） | 模型声明 | 登记受控 | Logic 迁移 |
|---|---|---|---|---|---|---|---|
| 用户管理 | `sys_user` | `User` | `id` | 经 `sys_user_dept` | [x] 参与 | [x] 已登记 | [x] 已完成 |
| 部门管理 | `sys_dept` | `Dept` | 我所属部门 | 部门子树（`id IN`） | [x] 参与 | [~] 不登记（已定） | [x] 已完成 |
| 附件管理 | `sys_file` | `File` | `create_by` | 经 `create_by` → `sys_user_dept` | [x] 参与 | [x] 已登记 | [x] 已完成 |
| 定时任务 | `sys_crontab` | `Crontab` | — | — | [x] 参与（`no_baseline`） | [x] 已登记 | [x] 已完成 |
| 操作日志 | `sys_log` | `OperationLog` | `user_id` | 可见部门成员 | [x] 参与 | [x] 已登记 | [x] 已完成 |
| 角色管理 | `sys_role` | `Role` | — | — | [~] 不参与（已定） | [~] 不登记（已定） | [x] 已完成 |
| 岗位管理 | `sys_post` | `Post` | — | — | [~] 不参与 | [~] 不登记 | [x] 已完成 |
| 数据权限规则 | `sys_data_rule` | `DataRule` | — | — | [~] 不参与（已定） | [~] 不登记（已定） | [x] 已完成 |

未参与 = 模型里 `protected $dataScope = false;`（已全部落位）。

### 1.3 明确不纳入范围

| 表 | 原因 |
|---|---|
| `sys_menu` / `sys_role_node` | 权限节点树，按角色隔离，与数据权限正交（`perm-scan` / `menu-sync` 也直接查它） |
| `sys_config` | 全局配置，无归属维度 |
| `sys_dict_type` / `sys_dict_data` / `sys_category` | 公共基础数据，全系统共享 |
| `sys_data_scope_table` | 数据权限自身的元数据 |
| `sys_crontab_log` | 日志明细，随 `sys_crontab` 归属 |
| `sys_user_role` / `sys_user_dept` / `sys_user_post` / `sys_dept_role` | 关联表，本身就是范围计算的输入 |

### 1.4 已定案

- [x] **`sys_data_rule` 不登记受控**，且模型声明不参与（否则管理员可能看不到自己配的规则）。
- [x] **`sys_role` 不登记受控**（不出现在「目标表」下拉、不能配自定义规则）。
- [x] **`sys_dept` 不登记受控** → **已于 2026-09-17 变更**：在受控表界面登记了 `dept`
      （可配自定义规则）。两种状态代码都兼容，撤销只需在「受控表」里移除该行。
      注意事项：登记后 `data_scope=5`（自定义档）用户在部门管理页是 fail-closed（没配规则即看不到）；
      另外给部门配字段级规则（如隐藏 `name`）会让部门树显示空白名。
      其中 `sys_dept` **始终参与预设基线**：「本部门及以下」档下部门管理页只见自己子树。
      → 实现方式：受控表只控制自定义规则，「参与基线」由模型声明控制，两者解耦。
- [x] **部门管理页在「本部门及以下」档下只见自己子树** → 已实现并验证。
- [x] **个人中心走逃生口**（本部门档 + 未分配部门 = fail-closed 会读不到自己资料）。
      `ProfileLogic` 已全量迁移（含角色名 / 部门名两处名称解析），实测该场景下仍能读到自己。

---

## 2. P0 基建（已完成）

### 2.1 性能：请求级 scope plan

- [x] `DataScope` 增加**每请求解析一次的 scope plan**：受控表 / 角色 `data_scope` / `roleIds` / `postIds` / `deptIds` / 部门树 / 全部规则。
- [x] 缓存实现：`WeakMap` 以 **UserContext 实例**为键。
      用实例而不是「用户 id」，是因为 webman 常驻进程下静态数组会跨请求存活（权限变更不生效）；UserContext 每请求由 `CheckLogin` 重建，WeakMap 随其回收自动释放，天然是请求级缓存，也不存在 `spl_object_id` 复用串数据的风险。
- [x] 实测：同一请求内 10 次模型查询 → 数据权限相关 SQL **0 条**；无缓存对照 → **50 条**（5 张表 × 10 次；实际含 `sys_role`/`sys_dept` 约 7 条/次）。

### 2.2 `BaseModel` 全局作用域

- [x] `protected $globalScope = ['dataScope'];` + `public function scopeDataScope($query)`。
- [x] 判定顺序：**无用户 → 超管 → 模型声明 `false` → 应用 `DataScope::row()`**。
- [x] `DataScope::applyToModelQuery()` 作为统一入口，表名以模型为准。
- [x] `row()` 的 `owner` 现在也支持**回调**（`callable($query, UserContext $user)`），用于「语义不是某一列等于我」的表（部门表：仅本人 = 我所属的部门）。
- [x] 自定义档（`data_scope=5`）的 fail-closed：**已改为无条件成立**（见 §7.13）——
      原先限定「只对受控表生效」，会留下「参与基线却没登记受控」的静默放行口子。
- [x] 模型时间戳校正（模型化写入的前置条件，否则会拼出不存在的列）：
      `File` / `OperationLog` → `$updateTime = false`；`UserRole` / `UserDept` / `UserPost` / `DeptRole` / `RoleNode` → `$autoWriteTimestamp = false`。

### 2.3 逃生口与无用户场景

- [x] 逃生口沿用 think-orm 自带 `Model::withoutGlobalScope()`（返回不带作用域的 Query）。
- [x] 无用户场景固化在 `DataScope::applyToModelQuery()` 里统一跳过：登录链路（`AuthService::buildContext`）、CLI（`perm-scan` / `menu-sync` / `db-upgrade` / 定时任务进程）、`#[NoLogin]` 路由。

### 2.4 回归基线

- [x] 三档行为用例已验证：超管（不受限）、`data_scope=1`（全部）、无角色（退化为「仅本人」）。

---

## 3. P1 模块迁移（按功能清单）

> 完成定义：① Logic 里业务表查询改走模型；② 模型声明语义；③ 越权用例通过；④ 如需生效则登记受控表。
> 括号内为迁移前 `app/logic/*.php` 的 `Db::name(` 调用数（工作量指标）。

### 3.1 用户管理（`UserLogic`，19 处）

- [x] 查询改走 `User` / `UserRole` / `UserDept` / `UserPost` 模型
- [x] 删除 `applyDataScope()` 与 `assertInScope()` 里的手写选项 —— 作用域由模型提供
- [x] `assertExists()` / `assertUniqueUsername()` 用 `withoutGlobalScope()` 看全量（否则「看不见就当没重复」会写出脏索引）
- [x] 字段级规则 `field` / `stripRow` / `stripProtected` 调用点保留
- [x] 越权用例：`read` / `update` / `delete` / `resetPassword` 对越权 id 全部 403，且数据未被污染

### 3.2 角色管理（`RoleLogic`，14 处）—— 已完成

- [x] 查询改走 `Role` / `RoleNode` 模型（列表、子角色展开、角色树、节点授权、唯一性校验）
- [x] 超管角色保护、回收站占用标识提示、`fields_strict` 相关行为保持一致（回归验证 `read(1)` 正常）
- 说明：`Role` 声明不参与数据权限，`AuthService` 的角色解析（走 `Db::name`）不受影响

### 3.3 部门管理（`DeptLogic`，7 处）—— 已完成

- [x] 查询改走 `Dept` / `UserDept` 模型（含树构建）
- [x] **作用域过滤后树的根节点处理**：可见节点的父节点可能不在集合内（例如只看自己所属的二级部门），
      已把这类节点提升为顶层，否则整棵树会因为找不到 `parent_id = 0` 的根而变空
- [x] 「绑定部门」候选项改为 `DeptLogic::treeAll()`（全量）：规则是配置动作，不能因为看不见某个部门就绑不了
- [x] 越权改部门 → 403 且数据未被改动（已回归验证）

### 3.4 岗位管理（`PostLogic`，5 处）—— 已完成

- [x] 查询改走 `Post` / `UserPost` 模型（不参与数据权限，新增/更新/软删已回归）

### 3.5 附件管理（`FileLogic`，8 处）—— 已完成

- [x] 查询改走 `File` / `Category` 模型（列表、批量移动、软删除、回收站视图）
- [x] 上传的两处细节：去重查询走**带作用域**的查询（否则会把别人上传的附件路径「复用」给你）；
      写后回显直接用写入数据，不再按 id 回查（「本部门」档未分配部门时作用域 fail-closed，回查会查不到自己刚上传的记录）
- [x] 登记受控表（`file`）——同时把 `DashboardLogic` 里对附件的裸查询改成模型，否则检查命令会拦

### 3.6 定时任务（`CrontabLogic`，5 处）—— 已完成

- [x] 查询改走 `Crontab` / `CrontabLog` 模型（列表、执行日志、软删除）
- [x] 单条入口统一为 `assertInScope()`：越权 403、不存在 404
- [x] 调度进程（CLI）没有当前用户 → 作用域自动跳过，已确认
- [x] 核对 `params` 的形态变化：模型把 JSON 列解码为数组，前端 `openEdit` 与 `CrontabRunner::run`
      本就同时兼容 string / array 两种形态，无回归
- [x] 登记受控表（`crontab`）：它声明 `no_baseline`，自定义规则是唯一的隔离手段

### 3.7 操作日志（`LogLogic`，3 处）—— 已完成

- [x] 查询改走 `OperationLog` 模型（列表、删除、清空）
- [x] 删除 / 清空改为**带作用域**：只影响自己可见范围内的日志（越权日志既看不到也删不掉）
- [x] 写入侧 `middleware/OperationLog.php` 不经过模型查询，未受影响
- [x] 登记受控表（`log`）

### 3.8 数据权限规则（`DataRuleLogic`，11 处）—— 已完成

- [x] 查询改走 `DataRule` / `DataScopeTable` 模型（规则 CRUD + 受控表管理）
- [x] 「绑定岗位 / 角色 / 部门」候选项与名称翻译全部显式 `withoutGlobalScope()`
- 说明：`information_schema` 元数据查询保留 `Db::query()`（基础设施反射，不是业务表查询）

### 3.9 其他查询入口（受检查命令驱动，已完成 `sys_user` 相关部分）

- [x] `ProfileLogic`：读/改自己改为 `User::withoutGlobalScope()` + 注明理由
      （否则「本部门档 + 未分配部门」会被 fail-closed 挡住读自己资料）
- [x] `ProfileLogic` 的角色名 / 部门名解析改走 `Role` / `UserDept` / `Dept::withoutGlobalScope()` 模型
      （`dept` 登记受控后由检查命令抓出）
- [x] `AuthLogic`：登录查账号、更新登录信息改为 `User::withoutGlobalScope()`（登录时还没有当前用户）
- [x] `DataRuleLogic`：规则页的用户选择器与绑定名称翻译改为 `User::withoutGlobalScope()`
      （配置界面需要全量候选，否则已绑定的用户会显示成空名字）
- [x] `DashboardLogic`：全部计数改走模型，随数据范围聚合（原先只迁了用户数，其余仍是全量口径）
- [x] `RecycleLogic`：`file` / `menu` 两个类型分支的裸查询改走模型
      （`withTrashed()` / `onlyTrashed()`；表名驱动的通用路径 `self::query($meta)` 保留）
- [ ] 剩余「不参与数据权限」的模块（`ConfigLogic` / `MenuLogic` / `CategoryLogic` / `DictLogic` /
      `GeneratorLogic`）：迁移价值仅在于统一查询入口，**不影响数据权限**（模型都声明 `false`），
      可按需再迁；漏迁也不会被检查命令拦（它们不查受控表）

---

## 4. P2 防漏（已完成）

- [x] **生成器模板**：`GeneratorLogic` 改为生成「模型化 + 带数据权限声明」的骨架。
  - [x] Logic 模板：查询走 `{Model}::newScopedQuery()`（有软删列时自动带上回收站数据源开关），
        create 用 `withoutGlobalScope()`、update/delete 走带作用域的查询，不再生成 `Db::name()`
  - [x] 模型模板：按**表结构**生成声明 —— 有 `create_by` + `dept_id` → 默认语义；
        只有 `create_by` → 附带 `deptScope()` 方法；两者都没有 → `['no_baseline' => true]` + TODO
  - [x] 产物已用 `sys_file` / `sys_user` / `sys_crontab` 三种表结构验证，12 个产物全部通过 `php -l`
- [x] **检查命令 `cccms:data-scope-check`**（`support/DataScopeChecker.php` + `command/DataScopeCheckCommand.php`）
  - [x] R1：`app/controller/**` 不得出现 `Db::name` / `Db::table` / `Db::query`（查询必须下沉到 Logic）
  - [x] R2：受控表必须有模型且不得声明 `$dataScope = false`；Logic 层不得再用 `Db::name('该表')` 查询
  - [x] 自动发现新插件（以 `db/menu.php` 为判定），新插件自动纳入检查
  - [x] **首轮即抓出 10 处真实漏网**（见 §7.6），全部修复后通过
- [ ] 接入部署/CI 流程，失败即阻断
- 说明：R1 只扫控制器，没有把 `support/**` 一并禁止 —— 基础设施层（`SoftDelete` / `SysConfig` /
  `AuthService` / `CrontabRunner` / `MenuSyncer`）本来就需要直查库，一刀切只会逼出大量无意义的豁免。
  真正要防的是「业务查询绕过 Logic」，而业务查询的落点就是 Logic 与 Controller。
- 说明：主代码中找不到模型的受控表只报 `NOTICE` 不报错（基础系统不负责下游插件部署里登记的表）。

---

## 5. P3 可选优化（已完成）

### 5.1 软删除下沉 —— 已完成

- [x] **前置核对**：`delete_time` 是 `DATETIME`，严格模式下 `= ''` 直接报 `Incorrect DATETIME value`
      → 历史数据里**不可能存在空串**；现有 `['null','']` 的 `''` 部分从未生效（实际就是 `IS NULL`），
      与 trait 默认语义完全一致，可无损切换
- [x] `BaseModel` 使用 think-orm 的 `SoftDelete` trait；9 张没有 `delete_time` 的表声明 `$deleteTime = false`
- [x] Logic 层改为 trait 语义：列表 → `Model::newScopedQuery()`、回收站 → `Model::onlyTrashed()`、
      软删 → `Model::destroy($ids)`
- [x] **踩到的坑（已处理）**：模型默认条件 `delete_time IS NULL` 会让「故意操作已删行」的查询静默变成 0 行。
      已补 `withTrashed()` 的三处：`PermScanner`（恢复软删的按钮节点、纠正归属父节点）、
      `MenuSyncer`（复用回收站里的菜单节点）、用户 / 角色的唯一性校验（回收站账号仍占用用户名）
- [x] `support/SoftDelete.php` **未删除**，但职责收窄为「查询构造器直查」场景：
      回收站模块（跨表操作已删数据）、`AuthService` / `DataScope` / 调度进程等基础设施，
      以及尚未模型化的 `category` / `dict` / `menu` / `dashboard`。文档已注明「能用模型优先用模型」
- [x] 顺带修掉一个真实缺陷：回收站恢复时「唯一值是否被占用」把**待恢复行自己**算成占用，
      导致 user / role / post / dict_type / menu 根本无法恢复（查重未排除已删行）

### 5.2 字段级规则下沉 —— 已完成

- [x] 出参：`BaseModel::toArray()` 统一应用 `hidden` / `mask` / `encrypt`
      （原来区分列表与单条两套语义，是因为写入侧不设防；现在不需要了）
- [x] 入参：新增 `app/model/ScopedQuery.php`（`BaseModel::$query`），在 `insert` / `update` / `save` /
      `insertAll` 写库前剔除不可写字段。**没有用模型事件**：业务写入都是构造器调用，事件不会触发
- [x] 下线 Logic 层的 `DataScope::field()` / `stripRow()` / `stripProtected()` 调用与旧方法

---

## 6. P4 文档（已完成）

- [x] README 6.4「数据权限」重写为模型层机制（逐模型声明表、性能数据、边界与逃生口、新插件接入三步）
- [x] README 6.4「当前已接入：仅 `user` 表」更新为 5 张表 + 受控表登记说明
- [x] README 6.6「软删除与回收站」更新为 trait 语义 + `withTrashed()` 陷阱说明
- [x] README 十二「项目状态 / 遗留」更新；特性表与架构图里的旧描述同步
- [x] README 十一「常用命令」补 `cccms:data-scope-check`
- [x] 新插件接入指南写在 README 6.4 末尾（模型声明 → 登记受控表 → 跑检查）

---

## 7. 已完成与验证记录

### 7.1 数据权限基础能力

- [x] `DataScope::row()`：行级注入（五档基线 + 自定义规则叠加 + fail-closed）
- [x] `DataScope::applyFieldRules()` / `applyWriteRules()`：字段级规则（模型层调用；原 `field()` /
      `stripRow()` / `stripProtected()` 已在 P3.2 下线）
- [x] 受控表注册表 `sys_data_scope_table` + 管理界面
- [x] 超管旁路、角色 `data_scope` 继承、动态取值变量

### 7.2 相邻能力

- [x] 回收站数据源开关 `SoftDelete::listQuery($table, $params)`（`trashed=1`）
- [x] 各模块表格右上角回收站入口（`RecycleToggle` + `useRecycle`）

### 7.3 本轮（BaseModel 数据权限）落地清单

| 文件 | 改动 |
|---|---|
| `support/DataScope.php` | 请求级 scope plan（WeakMap）、`applyToModelQuery()`、`currentUser()`、`owner` 支持回调、`myDeptIds()` / `myDeptSubtreeIds()` 公共辅助、fail-closed 收紧为只对受控表 |
| `app/model/BaseModel.php` | `$globalScope = ['dataScope']` + `scopeDataScope()` + `$dataScope` 声明位 + `newScopedQuery()` |
| `app/model/User.php` | 参与：`owner=id`、`dept` 走 `sys_user_dept` |
| `app/model/Dept.php` | 参与：`owner` = 我所属部门、`dept` = 部门子树 |
| `app/model/File.php` | 参与：`owner=create_by`、`dept` 落在可见部门成员；`$updateTime = false` |
| `app/model/Crontab.php` | 参与（`no_baseline`） |
| `app/model/OperationLog.php` | 参与：`owner=user_id`、`dept` 落在可见部门成员；`$updateTime = false` |
| 其余 15 个模型 | `$dataScope = false`；5 个关联表同时 `$autoWriteTimestamp = false` |
| `app/logic/UserLogic.php` | 全量迁移到模型，删除手写 `applyDataScope` |

### 7.4 端到端验证结果（本地真实库）

用「新增独立测试用户 + 测试角色」的方式验证，不改动既有数据，跑完清理（已确认无残留）：

| 场景 | 结果 |
|---|---|
| 超管 | `SELECT * FROM sys_user`（无范围条件）✓ |
| 仅本人 | `User` → `WHERE id = '990099'`；`File` → `WHERE create_by = '990099'`；`Dept` → `WHERE id = '1'`(我所属部门) ✓ |
| 仅本人 | `Role` / `Menu` → 无任何条件（已声明不参与）✓ |
| 本部门及以下 | `Dept` → `WHERE id IN ('1','2')`（子树展开）✓ |
| 全部数据 / 无用户 | 无范围条件 ✓ |
| 逃生口 | `User::withoutGlobalScope()` → 无条件 ✓ |
| `UserLogic::paginate` | 仅本人档下可见 1 个用户（自己）✓ |
| 越权拦截 | `read` / `update` / `delete` / `resetPassword` 对他人 id 全部 403，目标数据未被修改 ✓ |
| 唯一性校验 | 仍能看全量（重名被正确拒绝）✓ |
| 请求级缓存 | 同请求 10 次模型查询 → 权限 SQL 0 条；无缓存对照 → 50 条 ✓ |

冒烟测试：`php webman cccms:perm-scan --check` → 扫描 83 个方法、校验通过。

### 7.5 关于 `ks_*` 表（已定案）

- [x] `sys_data_scope_table` 里的 6 张 `ks_*` 表（`ks_subject` / `ks_account` / `ks_asset` / `ks_rule` /
      `ks_alert` / `ks_batch_job`）属于**下游业务插件系统**，本仓库是基础系统、不负责它们的 Logic。
      检查命令对「主代码中找不到模型的受控表」只报 `NOTICE`，不计入错误，CI 不会被它们卡住。

### 7.6 本轮（P2）验证记录

**检查命令首轮就抓出 10 处真实漏网**（`sys_user` 被绕过模型的查询）：

| 文件 | 处数 | 处理 |
|---|---|---|
| `AuthLogic.php` | 2 | 登录链路，改 `User::withoutGlobalScope()` |
| `ProfileLogic.php` | 4 | 个人中心，改 `User::withoutGlobalScope()`（保留返回数组结构） |
| `DataRuleLogic.php` | 3 | 规则页用户选择器 / 绑定名称翻译，改 `User::withoutGlobalScope()` |
| `DashboardLogic.php` | 1 | 用户数改为按数据范围统计 |

修复后 `php webman cccms:data-scope-check` → **通过**（仅剩 6 条 `ks_*` 的 NOTICE）。

**运行时复验**（临时脚本，跑完即删，库中无残留）：

| 项 | 结果 |
|---|---|
| 受限用户（仅本人档 + 无部门）读/改自己的资料 | 正常（逃生口生效），返回类型仍是数组 |
| 规则页用户选择器（关键字 + ids 两条路径） | 均能拿到全量候选人 |
| 工作台用户数 | 受限用户 = 1（按范围）、超管 = 2 ✓ |

**生成器产物**：用 `sys_file`（有 create_by 无 dept_id）、`sys_user` / `sys_crontab`（无 create_by）三种表结构生成，
12 个 PHP 产物全部通过 `php -l`；三种数据权限声明均按预期生成。

### 7.7 本轮（P1 剩余模块）验证记录

迁移：**角色 / 部门 / 岗位 / 附件 / 定时任务 / 操作日志 / 数据权限规则 + 受控表管理**，
8 个 Logic 文件里的 `Db::name()` 已全部清零（`information_schema` 反射除外）。

三档数据范围下的运行时回归（临时数据，跑完清理，库中无残留）：

| 场景 | 结果 |
|---|---|
| 超管 | 部门树两级完整；岗位 / 日志 / 附件 / 角色 / 规则各列表正常；受控表 7 张、可添加 36 张 |
| 仅本人（所属部门 = 二级部门 #2） | 部门树 = `#2` **并被提升为顶层**（不提升会因找不到根而变空）；附件 / 日志 = 0（只看自己的）；岗位 / 角色仍全量（声明不参与） |
| 本部门及以下（所属部门 #2） | 部门树 = `#2`（该部门无下级） |
| 越权改部门 | `ApiException 无权操作该部门`（403），库中数据未被改动 |
| 写路径 | 岗位、部门的 新增 / 更新 / 软删除 全部正常，`delete_time` 正确写入 |
| 配置界面 | 规则页「绑定部门」候选仍为全量（`treeAll()`），不受数据范围收窄 |

`php webman cccms:data-scope-check` → 通过（仅 6 条 `ks_*` NOTICE）；
`php webman cccms:perm-scan --check` → 扫描 83 个方法、校验通过。

### 7.8 本轮（P3 + P4）验证记录

**软删除下沉**（临时数据，跑完清理）：

| 场景 | 结果 |
|---|---|
| `UserLogic::delete()` | `delete_time` 写入时间戳（软删，非物理删）✓ |
| 正常列表 / 回收站列表 | 分别排除 / 包含该行 ✓ |
| `RecycleLogic::restore()` | `delete_time` 置回 NULL ✓ |
| `RecycleLogic::forceDelete()` | 删除 1 行，库中无残留 ✓ |
| `Menu::where(...)->whereNotNull('delete_time')->update()` | **0 行**（未加 `withTrashed()` 的陷阱，实测确认）|
| 加 `withTrashed()` 后 | 1 行 ✓；普通查询仍自动排除已删行 ✓ |
| 8 个模块的回收站视图（`trashed=1`） | 全部正常，无报错 ✓ |
| 岗位写入链路（新增 → 更新 → 软删 → 回收站可见） | 正常 ✓ |

**字段级规则下沉**（构造带 `mask` / `readonly` 规则的临时数据）：

| 场景 | 结果 |
|---|---|
| `read()` / `paginate()` 出参 | `phone` = `138****0000`（掩码生效）✓ |
| `update(['phone' => '139...', 'nickname' => 'HACKED'])` | 调用成功但两者**均未被写入**（mask / readonly 字段被剔除）✓ |
| 超管 | 不受字段级规则影响 ✓ |

**全量校验**：`app` + `support` 全部 PHP 文件 `php -l` 通过（0 错误）；
`cccms:data-scope-check` 通过；`cccms:perm-scan --check` 通过；临时脚本与临时数据已清理。

### 7.9 登记受控表（收尾）

`file` / `log` / `crontab` 三张表登记进 `sys_data_scope_table`（走路 `DataScopeTableLogic::save()`，
与界面点「确定」同一条路径），现在受控表共 10 张（含下游插件的 6 张 `ks_*`）。

登记前先修掉了两处会让检查命令报错的裸查询：

- `DashboardLogic` 全部改为模型查询 —— 顺带修了一个真实的**口径泄漏**：
  附件/日志列表已按数据范围收窄，工作台却仍显示全量总数，等于绕开列表把总量漏出去。
  现在 `user` / `dept` / `file` / `log` 的计数都随当前用户范围聚合（实测：超管
  `user=2 dept=2 log=33` → 仅本人档用户 `user=1 dept=0 log=0`）。
  附带修正：菜单数原先没过滤软删（`Db::name('menu')` 无软删条件），现在按模型口径统计。
- `RecycleLogic::forceDelete` 里对 `file` / `menu` 的裸查询改为 `File::withTrashed()` /
  `Menu::onlyTrashed()`（要操作的正是回收站里的行），实测彻底删除后物理路径清理与
  `role_node` 授权清理都正常。

**如何撤销**：数据权限规则页 →「受控表」→ 对应行删除即可（规则不会丢，只是暂停生效）。

### 7.14 规则表单改造：绑定对象改成选项卡 + 已选列表

原表单把四个 `ArtNodePicker` 竖排（每个都带工具栏 + 至少 148px 的节点区），
表单很长、且「已选了什么」被埋在各自的块里。改为：

- **四个维度做成选项卡**（用户 / 岗位 / 角色 / 部门），一次只显示一个选择器；选项卡标题带已选数量
  （如「部门 (2)」），切到别的选项卡也知道哪些维度已选；
- **「添加到已选」按钮**：树里点选只改「待添加」草稿（`draft`），点按钮才写进规则 ——
  避免误点即改动。单选维度是「替换」语义，部门是「并入」（去重 + 按 id 排序）；
- **已选列表放在选项卡之外**，所以切换选项卡始终可见；按维度分组渲染（每维度一行，
  部门多选就是一行多个标签），每个标签可单独 `×` 移除，另有「清空全部」；
- 名称解析：岗位 / 角色 / 部门直接展平已加载的候选（角色与部门是多层树）；
  用户靠搜索结果缓存 + 编辑回显时的 `user_name` 兜底，因此标签永远显示名称而不是 `#id`。

实现要点：`draft`（待添加）与 `form.*`（已选）分开；移除标签时同步清掉草稿里的同一项，
避免「树里勾着、其实没添加」的错位。提交体仍是显式构造，草稿不会进 payload。

**验证情况**：`vue-tsc --noEmit` 通过（模板里引用的 ref / 函数全部解析成功）、
`npm run build` 成功（5.77s）。**界面外观与交互未能实测** —— 本机没有可供 Playwright 使用的
Chrome / Edge（chromium 安装被切到后台），所以布局与交互流程还没被眼睛验证过，需要人工确认。

### 7.15 去掉规则的「排序」字段（原本是个假旋钮）

用户问「排序有什么用，如果只是表格排序就去掉」。查证结论 —— **它既不是权重，界面上也看不见**：

| 检查 | 结果 |
|---|---|
| `DataRuleLogic::paginate()` | `order('sort','asc')->order('id','asc')` —— 只影响列表行序 |
| `DataScope` 运行时 | **完全没有 `sort`，也没有任何 `ORDER BY`** —— 影响不到规则如何生效 |
| 规则列表列 | **没有「排序」列**（岗位 / 菜单 / 部门那些都有，所以那里 sort 是有意义的） |

**顺带发现一个真问题**：字段级动作是**按顺序依次处理**的（`applyFieldRules` 逐条 switch），
原先取规则时不指定顺序，就取决于 DB 返回顺序 —— 同一字段被多条字段级规则命中时
（例如 `mask` 与 `encrypt`），结果会变成不可复现。已在 `plan()` 显式 `ORDER BY id ASC`，
固定为创建顺序。这条已写进 README。

改动：

- 表单去掉「排序」；`FIELDS` / `create()` / `update()` 不再接受 `sort`（老前端多传会被忽略而不是报错）
- 列表固定 `order('id','asc')`
- `db/schema.sql` 删除该列；开发库执行 `ALTER TABLE sys_data_rule DROP COLUMN sort`
  （删列前实测 `sort <> 0` 的规则为 0 条，无数据影响；**其他环境的库按需执行同一句 ALTER**，
  不执行也不影响 —— 代码已完全忽略该列）
- 修正 README 里**我之前写错的一句**：「多条规则…（按 `sort` 升序）叠加」—— 运行时并不按 sort 排

验证：新建/更新（含多传 `sort`）/列表顺序 / `plan()` 取规则的 SQL（实测
`SELECT * FROM sys_data_rule WHERE delete_time IS NULL ORDER BY id ASC`）全部通过。

### 7.13 规则冲突体检 + 自定义档语义收紧

**需求**：用户问「规则1（且）允许看数据1」与「规则2（或）不允许看数据1」同时命中时以哪个为主。

**实测结论**：不存在「以哪个为主」—— 两条规则是 **AND 叠加**，实测 SQL 为
``WHERE ( `id` = '1' AND `id` != '1' )`` → **0 行**。也就是「什么都看不到」：
结果是 fail-closed（安全，不会误放开），但**静默**，管理员只会看到页面空白。

**指令 1 已落地**：自定义档（`data_scope=5`）的 fail-closed 改为**无条件成立** ——
「自定义规则没填 = 没有任何权限」，不再限定只对受控表生效。
原先那个限定会留下「模型参与基线却没登记受控」的静默放行口子；某张表不该隔离的正确做法是
模型声明 `$dataScope = false`。实测：自定义档用户 `user=0 file=0 dept=0`，
而规则表 `DataRule=2`（声明不参与）仍可见，否则用户改不了自己的规则。

**新增冲突体检**（`support/RuleConflict.php` + `cccms:data-rule-check` + 列表「检测」列）四类：

| 类别 | 含义 | 严重度 |
|---|---|---|
| `row_unsat` | 行级条件互斥 → 命中者看不到任何数据 | 错误（命令退出码 1） |
| `field_dup` | 同字段多个字段级动作 → 顺序叠加而非取最严（mask+encrypt 会产出「被掩码的密文」） | 提示 |
| `never_hit` | 取值不完整（`IN` 空 / `BETWEEN` 少值）→ 规则被静默跳过 | 提示 |
| `not_guarded` | 目标表未登记受控 → 规则不会执行 | 提示 |

判定原则「**只报可证明为空的组合，宁可漏报不误报**」：

- 只比较**同绑定项**的规则，或含**全局规则**（命中所有人）的组合；
- 只处理静态取值，`like` / 非数字比较一律跳过（判不了就不下结论）；
- 用**约束求交**而不是比字符串：`in` 把取值当集合求交、`between` 当区间求交、`!=` 当排除集，
  因此能抓到「三条规则两两有交集、合起来为空」这种组合
  （实测：`in 1,2` + `in 2,3` + `!= 2` → 报出；`in 1,2` + `in 2,3` → 不报）；
- 不同绑定项（如「部门 A」vs「岗位 P」）也有人可能同时命中，但需要查用户全量才能判定，不检。

**开发过程中被抓出的自身缺陷**：最初把 `bind_mode` 也放进了「同一批人」的分组键，
于是「规则1 用且、规则2 用或」被判成两批人而漏检 —— 而这正是用户举的例子。
反例实测后修正：同绑定项的规则无论「且」还是「或」，命中集合必然互为子集/超集
（交集 ⊆ 并集），因此绑定项相同即可比较，`bind_mode` 不能进分组键。

23 个判定用例 + 端到端（命令行 / 列表接口 / 运行时 SQL）全部通过；
`cccms:data-rule-check` 在冲突场景下退出码 1，列表两条规则均显示「检测：冲突」。

### 7.10 文案订正 + 收尾发现的漏网

「受控表」弹窗里那句提示还是改造前的写法（"前提是该表的业务逻辑已调用 DataScope"），
与现在的模型层机制不符，且会让人误以为**未登记的表没有数据权限**。已改为：

> 只有登记在这里的表才会出现在「目标表」候选中……登记的前提是该表已接入数据权限（模型声明参与、查询走模型）……
> 登记只决定「能不能配自定义规则」。未登记的表（如部门）并非没有数据权限，只是按角色档位套用预设基线。

同步订正的还有：`DataScopeTableLogic` 类注释、`db/schema.sql` 表注释、`DataScope::canGuard()` 注释、
`api/dataRule.ts` 的分节注释。另外 `db/seed.sql` 原来只登记了 `user`，新装的库拿不到 `file` / `log` /
`crontab` 的登记（与开发库不一致），已补齐并验证可重复执行（唯一键 + `INSERT IGNORE`）。

**订正文案时被检查命令抓出的真实漏网**：`dept` 在受控表界面被登记后，`cccms:data-scope-check`
立刻报出 `ProfileLogic.php:133` 仍在用 `Db::name('dept')`。已一并迁移：

- `ProfileLogic` 的角色名 / 部门名解析改走 `Role` / `UserDept` / `Dept::withoutGlobalScope()`；
- 实测「本部门档 + 未分配部门」（作用域 fail-closed、用户表可见行数为 0）下个人中心仍能读到自己，
  部门名为空数组而不是报错。

这正好演示了受控表的用法：**登记 = 纳入检查**。

### 7.11 规则绑定语义（答疑 + 界面提示订正）

界面原来只在「指定部门」那一条末尾提了半句"四项命中任意一项即生效"，看前三个绑定项的人
很容易读成「且」。已把提示提到四项之前的公共位置，并把角色那条补成「只认直接分配」。

运行时实测（临时数据，跑完清理；用户放在「自定义规则」档 5，用「查得到 / 查不到」反推绑定是否命中）：

| 场景 | 可见行数 | 结论 |
|---|---|---|
| 无规则 | 0 | 自定义档 fail-closed 生效 |
| 只绑用户，绑的是别人 | 0 | 绑定未命中 |
| 只绑岗位，绑的是自己有的岗位 | 1 | 命中 |
| **同时绑「别的用户」+「自己有的岗位」** | **1** | **是「或」**：若不是自己那个用户，靠岗位也能命中 |
| 同时绑「别的用户」+「自己没有的岗位」 | 0 | 两项都不命中 → 不生效 |
| 只绑部门，绑的是自己所在部门 | 1 | 命中 |
| **同时绑「别的用户」+「自己的部门」** | **1** | **是「或」** |
| 两条规则分别命中但条件互斥（`id=1` 且 `id=2`） | 0 | **规则之间是「与」** |

### 7.12 新增「绑定关系：或 / 且」开关

需求：单条规则内部四项绑定允许选「且」，用于表达「张三 且 在客服岗」。

- 表结构：`sys_data_rule.bind_mode varchar(8) NOT NULL DEFAULT 'or'`（`or` = 历史行为，向后兼容）；
  新装走 `db/schema.sql`，存量库走 `cccms:db-upgrade`（已实测加列成功）。
- 判定（`DataScope::rules()`）：先按**已填写**的维度逐项求命中结果，再按 `bind_mode` 组合 ——
  `and` 用 `!in_array(false, $hits)`，`or` 用 `in_array(true, $hits)`；**未填写的维度不入 `$hits`**，
  所以「只绑一项 + 且」等价于「只绑一项 + 或」（这是刻意的，否则只绑一项的 and 规则永不生效）。
- 归一化：`strtolower(trim())` 后校验。**大写 `AND` 会被当作「且」**（大小写不敏感），
  非法值兜底为 `or`。兜底方向选 `or` 的理由：与迁移前行为一致（老数据没有该列）；
  而大小写不敏感是为了避免「手写 SQL 敲成 AND 却被降级成更宽的或」。
- 保存入口 `DataRuleLogic::prepare()` 用 `DataScope::BIND_MODES` 白名单校验，非法值 422；
  列表 `decorate()` 回传 `bind_mode` + `bind_mode_label`，前端在「绑定对象」列打头显示 `且` / `或`。

运行时实测（探针用户有「岗位A」、部门 #1；规则统一把 user 表收窄成 `id = 1`，命中即可见 1 行）：

| 场景 | or | and |
|---|---|---|
| 无规则 | 0 | 0 |
| 只绑「自己有的岗位A」 | 1 | **1**（未填项不参与） |
| 「别人」+「自己的岗位A」 | **1** | **0** |
| 「自己」+「自己的岗位A」 | — | 1 |
| 「自己的岗位A」+「自己的部门#1」 | — | 1 |
| 「自己的岗位A」+「别人的部门#2」 | — | **0** |
| 「自己」+岗位A+部门#1（三项） | — | 1 |
| 四项全绑且全中 | — | 1 |
| 四项全绑、角色绑错 | — | **0** |
| 四项都不填（全局） | 1 | 1 |
| `bind_mode='AND'`（大写） | — | 按 and 生效 |
| `bind_mode='xxx'`（非法） | — | 兜底按 or |

**顺带发现的口径差异（待决策，未改动）**：规则的角色绑定用 `$plan['roleIds']`，即
`sys_user_role` 的**直接分配**；而鉴权用的是 `AuthService::effectiveRoleIds()`（直连 + 祖先角色）。
后果：把规则绑到父角色，只拥有子角色的用户**不会**命中。而部门绑定是**含下级**的
（绑「总公司」覆盖「研发部」），两者直觉不一致。
若要与鉴权/部门口径统一，做法是给 plan 增加 `effectiveRoleIds` 并在 `rules()` 的绑定判定里使用
（`{role.ids}` 动态变量与角色档位 `data_scope` 是否跟随，需要单独确认——
档位若跟随祖先角色，子角色用户可能因父角色而放大范围，风险方向不同）。

---

## 8. 验收标准

- [x] 受控表上的**任意**查询方式都带范围：列表、详情、统计、`count()`、写入（已逐模块回归）
- [x] 非受控表（§1.3）行为与迁移前一致，无额外 SQL
- [ ] 超管无感知；无角色用户仍退化为「仅本人」
- [x] 单请求模型查询 ≥10 次时，`DataScope` 相关 SQL 总数**不随查询数线性增长**
- [x] 登录、CLI、`#[NoLogin]` 路由功能不受影响
- [x] `cccms:data-scope-check` 无错误；故意漏接能被拦住（首轮已实测抓出 10 处）

---

## 9. 文档梳理发现的改进项（待处理）

> 来源：本轮编写 `docs/`（安装 → 框架 → 权限 → 开发规范 → 前端 → FAQ）并重写 `README.md` 时，对照代码逐项核对发现的**过期描述、缺陷、工程化缺口与可选优化**。
> 已同步落地的：`README.md` 已改为 GitHub 风格主页；目录结构描述已修正为「插件配置在 `plugin/{插件}/config/`」。
> 优先级：P0 = 影响正确性 / 安全，P1 = 影响体验或易踩坑，P2 = 工程化，P3 = 可选增强。

### 9.1 P0 · 正确性与安全

- [ ] **`support/DataScope.php` 类注释与实现矛盾**（第 27 行）：
      注释写「5 自定义规则……（没有规则 = 等同全部）」，但实现为
      `if ($scope === 5 && $rules === []) { $query->whereRaw('1 = 0'); }`（fail-closed，看不到任何数据）。
      该矛盾会直接误导接入者。请把注释改为「没有规则 = 看不到任何数据」，并说明
      「某表不该隔离的正确做法是模型声明 `$dataScope = false`」（与 [docs/06-数据权限.md](docs/06-数据权限.md)、§7.13 对齐）。

- [ ] **登出未做服务端失效**：`AuthLogic::logout()` 为空实现（注释 TODO Redis 黑名单）。
      客户端丢弃 token 后，旧 token 在有效期内仍可用。建议：按 `jti` 写 Redis 黑名单
      （TTL = 剩余有效期），或按用户维度失效（复用/扩展 `SessionGuard`）。

- [ ] **`server/config/app.php` 的 `debug` 硬编码为 `true`**，无环境变量开关。
      生产忘记关闭会通过 `ExceptionHandler` 透出异常信息。建议改为
      `'debug' => (bool)getenv('APP_DEBUG')`（默认 false 更安全）。

### 9.2 P1 · 易踩坑与不一致

- [ ] **`db/seed.sql` 注释过期**（第 44-45 行）：仍写「配置项……**尚未被后端业务代码消费**
      （即改值暂时不会改变系统行为）」，但 24 项已全部接入。请改写为「已全部接入，保存后立即生效」。

- [ ] **`log.auto_clean` / `log.keep_days` 默认不生效**：两者由 `command/task/DemoTask` 读取，
      但 `seed.sql` **未预置该定时任务**，新装环境日志不会被自动清理。
      建议 seed 一条每天执行的 `DemoTask`（或把日志清理做成框架内置默认任务）。

- [ ] **`command/task/DemoTask.php` 命名不当**：它是生产用途的「清理历史操作日志」任务，
      却叫 Demo。建议重命名为 `LogCleanTask`（注意：已存在的 `sys_crontab.target` 需同步改写）。

- [ ] **`sys_dept` 是否默认登记受控表不一致**：开发库已登记（见 §1.4 与 §7.10），
      但 `seed.sql` 只登记 `user/file/log/crontab`，而 [docs/06-数据权限.md](docs/06-数据权限.md) 的表里 `dept` 标「已登记」。
      建议二选一并统一：① seed 补上 `dept`；② 统一为「不默认登记」并修正文档状态列。

- [ ] **规则角色绑定口径不一致**（§7.12 已记录，未改动）：规则用直连角色，
      鉴权用「直连 + 祖先」。绑到父角色时子角色用户不会命中，而部门绑定却是含下级的。
      建议统一为含祖先（给 plan 增加 `effectiveRoleIds`），或至少在前端规则表单显式提示「只认直接分配」。

### 9.3 P2 · 工程化与 CI

- [ ] **接入 CI 检查**（§4 中未完成项）：`.github/workflows/php.yml` 的测试步骤全部被注释。
      以下命令都有退出码，可直接作为 CI 步骤：
      `cccms:perm-scan --check`、`cccms:data-scope-check`、`cccms:data-rule-check`。
- [ ] **补后端自动化测试**：`composer.json` 无 `test` 脚本。至少覆盖：
      鉴权矩阵（未声明 → 500 / NoAuth / NoLogin）、数据权限三档行为、回收站 `withTrashed` 陷阱。
- [ ] **前端补 ESLint / Prettier（+ 可选 stylelint）**：当前只有 `vue-tsc`。
      建议用 lint 固化「组件名 = 菜单 slug」「颜色只用 `--art-*`」等约定。
- [ ] **修正 `Dockerfile` 的命令拼接缺陷**：`RUN docker-php-source delete \` 换行后接
      `rm -rf /var/cache/apk/*`，反斜杠续行会把两条命令并成一条，`rm` 被当作
      `docker-php-source` 的参数而**不会执行**（apk 缓存未清理）。改为 `&&` 或拆成两条 `RUN`。
      另：镜像未安装 `redis` 扩展（当前靠 predis 回退）与 `event` 扩展。
- [ ] **完善 Docker 部署**：`docker-compose.yml` 只有 webman 一个服务且镜像不 COPY 代码
      （依赖 volume 挂载），不含 MySQL / Redis，生产不可直接用。
      建议补 `.dockerignore`、多阶段构建（`composer install --no-dev`）与
      webman + mysql + redis 的完整编排。

### 9.4 P3 · 可选增强（借鉴同类框架）

- [ ] **在线用户 / 强制下线**：`SessionGuard` 目前只能全局分界线 + 维护模式，无法按用户踢下线
      （RuoYi / JeecgBoot 常见能力）。需要会话索引表或 Redis 按用户维护。
- [ ] **登录日志模块**：目前登录失败只在 Redis 计数，成功只更新 `login_time` / `login_ip`。
      建议独立记录登录成功 / 失败（含 IP、UA），便于审计。
- [ ] **通用导入 / 导出（Excel）**：用户、日志、字典等模块常需导出；代码生成器也可一并生成导出按钮。
- [ ] **通知公告 / 站内信**：可作为「新建业务插件」的标准示例，用来演示插件纳管全流程。
- [ ] **定时任务增强**：当前调度进程内**串行**执行，长任务会顺延后续 tick。
      建议补「超时中断 / 重叠保护 / 失败重试 / 分组」，避免互相拖累。
- [ ] **权限集合缓存**（`CheckLogin` 的 TODO）：改走 Redis + 版本号失效，
      避免每次请求实时查角色 / 节点。
- [ ] **请求链路 ID（traceId）**：写入 `sys_log` 与日志文件，便于把一次请求的多条记录串起来。
- [ ] **前端列表体验**：列宽拖拽记忆（当前只记显隐）、面包屑可点击、快捷导航搜索。
- [ ] **清理 `UserContext::fromClaims()`**：仍保留 claims 携带权限快照的骨架实现（注释 TODO），
      与当前实际链路（`AuthService::buildContext` 实时加载）重复，建议移除或注明仅内部使用。
- [ ] **前端编码能力说明**：后端 `Result` 支持 `jsonp / xml / view`，但前端 `request.ts`
      无 `format` / `responseType` 适配。若这些编码只用于对接三方 / 兼容旧系统，请在
      [docs/04-框架介绍.md](docs/04-框架介绍.md) 明确「仅后端能力，前端不使用」。

### 9.5 文档维护约定

- [x] 新增 `docs/` 目录：`README`（导航 + 编写规范）+ 01~12 共 13 份文档，覆盖
      项目介绍 / 安装部署 / 目录结构 / 框架介绍 / 鉴权与权限 / 数据权限 / 功能模块 /
      后端规范 / 前端规范 / 前端组件参考 / 常用命令与运维 / FAQ。
- [x] `README.md` 重写为 GitHub 风格主页（简介 / 特性 / 技术栈 / 目录 / 快速开始 / 安全要点 / 文档索引 / 命令 / 贡献 / 协议 / 致谢）。
- [ ] **约定**：改动机制、接口、命令、配置或目录结构时，**同步更新对应文档**；
      `docs/README.md` §三 已写明文档编写规范与更新要求。
