# 更新日志

> 本文档记录 CCCMS 的版本变更与待办事项，由原根目录 `待办.md` 迁移而来。
>
> **格式参考** [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)：版本内按
> `新增 / 变更 / 修复 / 移除 / 安全 / 文档 / 工程化` 分类；版本号遵循语义化版本（SemVer）。
> **状态图例**：`[x]` 已完成 · `[ ]` 待办 · `[~]` 明确不做（附原因） · `[?]` 待决策。

---

## [未发布]（Unreleased）

> 尚未落地的改进项。完成后请移动到对应版本号下并勾选。

### 安全

- **移除 JWT 密钥的公开默认值兜底（P0-1）**：`config/auth.php` 不再回退到仓库内可见的默认密钥，
  `JWT_SECRET` 未配置或不足 32 字节时由新增的 `support/SecurityBootstrap.php` 在**启动期**直接失败，
  而不是静默用弱密钥签发令牌。同时修复根因：`composer.json` 缺少 `vlucas/phpdotenv`，
  导致 `.env` **从未被加载**、所有配置静默回落到硬编码默认值。
- **消除同域存储型 XSS（P0-2）**：上传白名单移除 `svg`；`StorageDriver` 新增危险扩展名硬拒绝名单
  （`svg` / `html` / `xml` / `mhtml` 等，**优先于后台白名单**）与魔数内容校验；
  新增静态文件中间件 `app/middleware/StorageGuard.php`，`/storage/` 下非图片 / PDF 强制
  `Content-Disposition: attachment` 并全局下发 `X-Content-Type-Options: nosniff`。
- **登录失败锁定增加 IP 维度（P1-1）**：`LoginThrottle` 由单一账号维度拆为
  `cccms:login_fail:user:{用户名}` 与 `cccms:login_fail:ip:{IP}` 双维度；IP 维度达阈值只限流该 IP、
  **不锁账号**，避免攻击者用错误密码锁死他人账号。新增配置 `security.login_fail_ip_limit`（默认 20）。
- **全站接口限流（P1-2）**：新增 `support/RateLimiter.php`（Redis 固定窗口计数）+ 中间件
  `app/middleware/RateLimit.php`，按「每用户每路由」限流，重接口（`export` / `import` / `generate`）
  单独更严的阈值，超限返回 `429` + `Retry-After` / `X-RateLimit-*` 响应头；
  Redis 不可用时 **fail-open**（不因缓存故障挡业务）。新增 4 项 `security.rate_limit_*` 配置。
- **强化密码策略（P1-4）**：`PasswordPolicy` 新增长度上限（默认 64，避免超长口令放大 bcrypt 开销）、
  内置弱口令黑名单（约 90 条，含中文环境常见口令）、字符类别数要求（默认 2 类）、
  「不得与用户名 / 昵称 / 邮箱相同或包含」；并**补齐了 `UserLogic::update` 此前完全缺失的策略校验**。
  新增配置 `security.password_max_length` / `security.password_strength`。
- **CORS 白名单可配置化（P1-6）**：新增 `config/cors.php`（`CORS_ORIGIN`，默认留空 = 仅同源）；
  `Cors` 中间件改为**命中白名单才下发整组 CORS 头**并原样回显 Origin（不返回 `*`），
  带 `Origin` 时无条件 `Vary: Origin`。
- **令牌生命周期改造：缩短 TTL + 滑动续期（P1-3）**：`security.token_ttl` 默认值由 **7 天缩短为 2 小时**
  （令牌存 localStorage，TTL 就是 XSS 一旦发生攻击者能用的窗口长度）；新增滑动续期 ——
  `CheckLogin` 在剩余有效期不足 TTL 的 1/3 时签发新令牌并经响应头 `X-Refresh-Token` 下发，
  前端 `api/request.ts` 静默替换，活跃用户不掉线。续期后 `OnlineSession::rotate()` 把在线会话
  迁到新 `jti`（否则「强制下线」踢的是已废弃的旧令牌）；旧令牌**不写入黑名单**，避免并发请求被判 401。
- **收敛字段级 `encrypt` 密钥的下发范围（P1-5）**：`crypto_key` 改为**只下发给持有
  `cccms:data_rule*` 权限节点的管理员与超管**（`AuthLogic::mayDecryptFields()`），
  普通账号 `/auth/me` 不再返回；文档明确写出「`encrypt` 是存储加密、**不是**访问控制」，
  规则配置页选择 `encrypt` 动作时展示风险提示。

### 新增

- **列表批量操作（P2-1）**：用户 / 岗位 / 字典三个模块支持批量启停与批量删除，用户额外支持
  **批量分配角色 / 部门 / 岗位**（整体替换语义：传了哪个键就重设哪个关联）。新增 9 个接口
  （`POST /user/batchStatus|batchDelete|batchAssign`、`/post/batchStatus|batchDelete`、
  `/dict/batchStatus|batchDelete|batchStatusData|batchDeleteData`）与 9 个权限节点。
  统一语义为「**只作用于可见行 + 跳过并回报 + 不整批失败**」：越权行、超管账号、操作者自己、
  下挂用户的岗位一律计入 `skipped` 并回传 `{affected, skipped[]}`，前端降级为 warning 并说明原因。
- **角色复制 / 权限模板（P2-2）**：新增 `POST /role/copy` + 节点 `cccms:role:copy`。复制角色本体
  （**显式复制 `data_scope`** —— 档位刻意不继承，漏复制会静默落到默认「仅本人」）与**自身**
  `role_node` 授权，不复制子角色；`code` 自动派生 `_copy` / `_copy2`…；父角色做存在性与深度 ≤5 校验
  （同时兜住历史脏数据中的环）；勾「另存为模板」即复制为 `status=0` 的禁用角色。
- **密码找回（邮箱 / 短信）（P2-3）**：新增 `POST /auth/password/sendCode` 与
  `POST /auth/password/reset`（均 `#[NoLogin]`），支持用**用户名 / 邮箱 / 手机号**发起找回。
  通道以**可插拔适配器**实现（`support/Mailer.php` 零依赖 SMTP 客户端、`support/SmsSender.php`
  通用 HTTP 网关，参数模板支持 `{mobile}` / `{code}` / `{sign}` 占位符），核心逻辑集中在
  `support/PasswordReset.php`。安全设计：**不泄露账号存在性**（发送间隔 / 每日上限在查库**之前**
  按账号 md5 计数并 fail-closed，账号不存在时与成功同响应；通道故障只记日志不抛错，避免被用作枚举信号）；
  验证码 6 位数字**只存 bcrypt hash**，TTL 默认 300s、尝试上限 5、校验成功一次性消费；
  重置走 `PasswordPolicy` 且拒绝与旧口令相同；成功后 `TokenBlacklist::revokeUser()` +
  `OnlineSession::removeUser()` 作废该用户**全部**旧令牌与在线会话。新增 20 项配置
  （`security.reset_*` 5 项 + `mail.*` 8 项 + `sms.*` 7 项，敏感项 `type=password` 加密存储、
  接口只回 `has_value`）。前端登录页新增「忘记密码」入口 + 弹窗（账号 → 渠道单选 →
  验证码 + 60s 倒计时 → 新密码），**仅当 `/config/ui` 下发非空 `reset_channels` 时才渲染**
  （即 `security.reset_channel=off` 或通道未启用时入口不显示）。
- **「我的登录设备」自助管理（P2-5）**：新增 `GET /profile/sessions` 与
  `POST /profile/sessions/revoke`（登录即可，不产生权限节点）。用户仅能看到并注销**自己**的会话，
  归属校验失败统一返回「会话不存在或已失效」（避免探测他人会话），禁止注销当前会话；
  注销时 `TokenBlacklist::revoke()` + `OnlineSession::remove()` **两处同时生效**。个人中心新增
  「登录设备」卡片。
- **前后端 i18n 补全（P2-16，批次一：基础设施 + 后端 + 高频页）**：
  - **后端**：新增 `I18n::has()`（判断 key 是否存在，**刻意不写 missing 日志**——菜单 / 配置项
    「查不到」是正常分支，不是缺词）；`lang/{zh-CN,en-US}` 按命名空间拆为 22 个语言包
    （`menu` 126 条、`config_item` 44 条、`user` 36 条、`common` 30 条…），两语言 key 集合严格对齐。
    用户可见层提示语全部迁移到语言包（20 个控制器 + 18 个 Logic + 2 个中间件），
    `throw new ApiException('中文')` 归零。
  - **菜单与配置项标题**：`MenuLogic::localizeTitles()` 按 `menu.{slug}` 命中才覆盖标题，
    `ConfigLogic::localize()` 覆盖配置项名并新增 `group_label` 字段（分组名翻译；`group`
    **保持原值**——它是前端过滤键，翻译它会破坏过滤）。管理员自建菜单 / 配置项无对应 key，
    自动原样返回（用户录入数据不翻译）。`#[Permission(title:)]` 注解标题、`Log::` 日志与
    CSV 导出内容同样不翻译。
  - **前端**：语言包由 `zh-CN.ts` / `en-US.ts` 拆为 `src/locales/{zh-CN,en-US}/` 下 17 个模块文件
    （共 509 key × 2）；新增 `src/locales/title.ts` 的 `translateTitle()`，用 `i18n.global.te()`
    探测区分「前端 i18n key」与「后端已翻译标题」，供 `document.title` / 面包屑 / 标签页复用，
    `router` 导出 `syncDocumentTitle()` 统一刷新；标签页改存 i18n key，切换语言无需重建。
    菜单标题改由后端按请求语言下发，前端不再二次翻译（故前端无 `menu` 命名空间）。
  - **覆盖页面（10 个）**：user / role / dept / post / online / dict / config / menu / log / profile。
    列头 `ArtTableColumn[]` 与校验规则 `FormRules` 改为 `computed` 化（否则切换语言不刷新）；
    带变量的文案整句进语言包，不再用 `t()` 拼串（中英语序不同）。
  - **批次二（已收口）**：再覆盖 `data_rule` / `crontab` / `file` / `upgrade` / `generator` /
    `dashboard` / `notice` 7 个页面与公共组件 `CronEditor.vue` / `ArtNodePicker.vue`，
    两个语言包合计 **1001 key × 2**，`npm run i18n:check` 通过。组件里被数字 / 下拉框打断的句子
    （如「每 N 秒执行一次」）改用 `<i18n-t>` + 具名 slot，避免拼串导致语序错误；
    期间顺带修掉两处 `v-for="t in ..."` 遮蔽 i18n `t` 的隐患。
    **未做 i18n 的位置（刻意）**：备注、参数（JSON）等多行短文本域仅翻译 label / placeholder，
    控件形态与用户录入数据一律不动。
- **公共富文本编辑器组件（P2-19）**：新增 `components/core/ArtRichEditor.vue`（tiptap v3 +
  `@tiptap/extension-placeholder`），仅用于**长文录入**场景（当前接入通知公告正文），
  备注等短输入框**不使用**富文本。富文本存库为 HTML，因此设两道净化：后端保存侧
  `NoticeLogic::sanitizeContent()` 正则白名单 + 前端渲染侧 `utils/richText.ts` 的 `sanitizeHtml()`
  （DOMPurify，白名单标签 / 属性显式声明）；凡是 `v-html` 渲染富文本的位置（含顶栏「我的消息」
  详情）都必须先过 `sanitizeHtml`，防止历史脏数据触发存储型 XSS。新增依赖
  `@tiptap/vue-3` / `@tiptap/starter-kit` / `@tiptap/extension-placeholder` / `dompurify`。
- **多租户（P2-17）**：新增**单库共享表 + `tenant_id` 列**的租户隔离，`tenant_id = 0` 为**平台租户**
  （虚拟租户，**不落库**，只有真实租户 `id >= 1` 进 `sys_tenant`）。租户是**硬边界**，
  作为**最外层**全局作用域叠加在数据权限之上：`BaseModel::$globalScope = ['tenant', 'dataScope']`，
  租户过滤先于档位过滤 —— 所以「超管 / `roleScope === 1` 直接放行」的数据权限逻辑
  **不可能**造成跨租户泄露（租户条件**刻意不放进** `DataScope::row()`）。
  新增 `support/TenantContext.php`（生效租户 / `PLATFORM_ID` / `usable()` / `TABLES` 受控表清单 /
  `participates()` / `table()` / `applyToQuery()` / `stamp()`）、`app/model/Tenant.php`、
  `app/logic/TenantLogic.php`、`app/controller/TenantController.php` 与 `sys_tenant` 表。
  **隔离范围 12 张表**：`user` / `dept` / `role` / `post` / `data_rule` / `data_scope_table` /
  `notice` / `file` / `category` / `dict_type` / `dict_data` / `crontab`，各补 `tenant_id` + `idx_tenant`；
  `sys_menu` / `sys_config` / `sys_log` / `sys_crontab_log` 与**全部关联表**（`user_role` /
  `user_dept` / `user_post` / `dept_role` / `role_node` / `notice_target` / `notice_read`）
  保持**平台级**（越权关联由业务层同租户校验兜住）。
  - **模型侧**：`$tenantScope` 为**显式 opt-in**（代码生成器产出的 `ks_*` 插件表没有该列，
    不能默认开启）；写入侧 `ScopedQuery` 统一 `stamp()` 补齐 `tenant_id`，
    落库数据**无法「忘记带租户」**；`Db::name()` 直查构造器绕过模型作用域，
    一律改用 `TenantContext::table()` / `applyToQuery()` / `stamp()`。
  - **逃生口收窄**：覆写 `BaseModel::withoutGlobalScope()` → **恒保留租户条件**，
    新增 `withoutAllScopes()` 才能跨租户（仅登录链路 / CLI / 迁移 / 全局唯一性查重使用，
    且模型软删除过滤仍在，需 `withTrashed()` 才含回收站）。历史代码里大量
    `withoutGlobalScope()` 因此**不会变成越权口子**。
  - **全局唯一键**：`username` 保持**全局唯一**（`uk_username` 不动，登录不解析租户）；
    `uk_code`（角色 / 岗位 / 租户）/ `uk_type` / `uk_table` 同属全局唯一，
    查重一律用 `withoutAllScopes()`（若按租户收敛会「租户内不重名」但撞唯一索引直接报错）；
    用户 / 角色 / 岗位的关联项（部门、角色、岗位、父角色）新增**同租户校验**。
  - **超管跨租户 = 显式切换**：`GET /auth/tenants` 列出可切换租户（含当前项标记），
    `POST /auth/switchTenant` 重签带 `tid` 声明的令牌
    （`TokenService::issue($userId, ['tid' => $tenantId])`）并经 `OnlineSession::rotate()`
    迁移会话 `jti`；`CheckLogin` 从令牌的 `tid` 声明取生效租户交给
    `AuthService::buildContext()`（内含 `TenantContext::usable()` 的存在性 / 启用 / 到期校验），
    租户不可用即 401；非超管携带与自身归属不一致的 `tid` 一律拒绝。
    租户管理接口**仅对「处于平台租户的超管」开放**（`TenantLogic::assertPlatformAdmin()`，
    超管切进某租户后即失去该入口），授权时不要把 `cccms:tenant*` 节点下发给租户内角色；
    平台租户行**不可编辑 / 不可删除**，被账号占用的租户拒绝删除。
  - **前端**：新增租户管理页 `pages/cccms/tenant/index.vue`（`row-key="id"`，平台行只读 + 提示）
    与用户下拉里的租户切换入口（`layouts/components/LayoutHeader.vue`，仅超管可见，二次确认后切换）。
    切换后 `window.location.reload()`：页面数据与 `keep_alive` 缓存都属旧租户，
    整体重载才是干净口径（`sys_menu` / `role_node` / `user_role` 均为平台级，菜单无需重建；
    令牌存 localStorage，重载不掉登录态）。
  - **升级**：`cccms:db-upgrade` 幂等建 `sys_tenant` + 12 张表补 `tenant_id` / `idx_tenant`
    （已在默认值上的旧值不重复更新）。
- **快捷导航支持收藏与排序**：顶栏「快捷导航」（**Ctrl / ⌘ + K**）对话框拆为上下两区 ——
  上半区是**本机收藏**（可上移 / 下移排序、可取消收藏，空态有提示），下半区是全部菜单 / 搜索结果，
  每行右侧星标一键切换收藏。收藏存 `localStorage['cccms_quick_nav_fav:']`（键追加
  `{用户 id}:{租户 id}`），按**用户 + 租户**双重隔离：换账号或切换租户后各用各的收藏；
  菜单里已不存在的收藏路径在回查时自动跳过，不产生死链。
  `menuStore` 新增 `flatMenus` / `favoriteMenus` / `isFavorite()` / `toggleFavorite()` / `moveFavorite()` /
  `reorderFavorite()`，并作为顶栏快捷导航、标签栏星标、工作台「我的收藏」宫格**三处的唯一数据源**
  （不再各自扁平化菜单树；登出时 `reset()` 一并清空收藏）。
- **时间范围筛选支持常用区间与时分秒**：所有 `filter.type = 'date'` 的列头 / 顶部搜索项统一改为
  `el-date-picker` 的 **`datetimerange`**，值格式 `YYYY-MM-DD HH:mm:ss`，`defaultTime` 让手动选择
  默认落在 `00:00:00 ~ 23:59:59`；并新增 7 个常用区间快捷项：**今天 / 昨天 / 最近 3 天 / 最近 7 天 /
  最近 30 天 / 本月 / 上月**（`ArtTable` 内 `dateShortcuts`，文案在 `table` 命名空间：
  `timeToday` / `timeYesterday` / `timeLast3Days` / `timeLast7Days` / `timeLast30Days` /
  `timeThisMonth` / `timeLastMonth`）。列头弹层宽度按 `date` 类型放宽到 760px。
  后端 `FilterInput::range()` 相应改为**直接返回可比较的 `Y-m-d H:i:s`**：只给到日期时按
  「起始 `00:00:00` / 结束 `23:59:59`」补边界（保持「按天筛选含当天」的旧语义），带时分秒时原样保留；
  6 个 Logic（log / user / file / crontab / tenant / notice）去掉各自拼接 ` 00:00:00` / ` 23:59:59` 的写法。
- **补齐缺失的时间范围筛选列**：**在线用户**的 `login_at` / `last_at` / `expire_at` 三列、
  **公告**的 `publish_at` / `expire_at` 两列此前没有筛选入口，现在每列各配一组独立的
  `startKey` / `endKey`（如 `login_start` / `login_end`），避免同一页多个时间条件互相覆盖。
  在线用户的数据在 Redis 里，`OnlineSession` 新增 `RANGE_FIELDS`（`login` → `login_at`、
  `active` → `last_at`、`expire` → `exp`）与 `timestamp()` 归一化，`match()` 由「只做文本包含」
  扩展为**文本包含 + 时间戳区间**的 AND 匹配（比较用的是 Hash 里的原始时间戳，不是格式化后的字符串）；
  `NoticeLogic` 新增 `publish_at` / `expire_at` 两段时间范围查询。

### 变更

- **工作台统计扩充，「我的收藏」宫格可拖拽排序**：`GET /dashboard/stats` 的 `counts` 在原 6 项基础上新增
  在线用户 / 未读消息 / 字典 / 定时任务 / 数据规则 / 租户。任一模块计数异常按 0 兜底，
  不让单表故障拖垮整个工作台。原「常用功能」（取可见菜单前 12 个）改为**「我的收藏」宫格** ——
  与顶栏快捷导航、标签栏星标**共用同一份收藏**，顺序即收藏顺序，卡片上可直接**拖拽排序**
  （原生 HTML5 拖拽，未引入第三方库）；未收藏任何菜单时显示引导文案。
- **工作台权限隔离（两层）**：统计卡由「固定 8 张 + 5 张按权限追加」改为**全部 13 项逐项按权限节点下发**
  —— `user` / `dept` / `role` / `post` / `menu` / `file` / `log`(`today_log`) / `online` / `dict` /
  `crontab` / `data_rule` / `tenant` 各自登记了「能进对应列表页」所需的节点
  （如 `dept` 用 `cccms:dept:tree`、`menu` 用 `cccms:menu:tree`），**无权限时该键不下发**，
  前端按「键是否存在」决定是否渲染卡片。于是权限只需两层：**超管**因
  `UserContext::hasPermission()` 直通而拿到全部；**其他账号**只看到自己有权限的模块，
  数字本身再按数据范围（`data_scope`）收窄。图表同理 —— 无日志 / 附件列表页权限时
  `log_trend.dates` / `file_types` 返回空数组，趋势图与饼图整卡不渲染。
  `notice_unread` 是本人消息数，恒返回。
- **标签栏新增收藏按钮**：标签栏刷新按钮**旁边**加星标，切换「当前标签页是否收藏」，
  已收藏为金色实心星 + 底色、未收藏为灰色轮廓星；不在菜单里的页面（如个人中心）不渲染该按钮。
  与顶栏快捷导航、工作台「我的收藏」宫格**三处共用 `menuStore` 的同一份收藏**，任一处改动其余两处即时同步。
- **顶栏按钮整理，消息与系统操作收进用户下拉**：顶栏 `header-right` 由「快捷导航 / 刷新页 /
  我的消息 / 系统同步 / 清理缓存 / 租户切换 / 主题设置 / 全屏」精简为**快捷导航 + 主题设置 + 全屏
  三个图标**（标签栏关闭时才多一个「刷新当前页」），其余全部移入**右下角用户名下拉**：
  - 未读数从铃铛图标改为**用户头像角标**（> 99 显示 `99+`），铃铛图标移除；
  - 「我的消息」成为下拉菜单项（右侧带红色未读计数）；
  - 原「系统同步 / 清理缓存」拆出的 4 项（整体同步 / 菜单同步 / 权限同步 / 清理缓存）以
    **分组标题 + 平铺项**呈现（Element Plus 2.x 的 `el-dropdown` 不支持二级子菜单），
    仍受 `v-auth="'cccms:config:refresh'"` 约束；下拉项命令用 `refresh:` / `tenant:` 前缀统一在
    `onUserCommand()` 内分发，避免为每种动作各写一个 handler；
  - 「切换租户」仅 `userStore.superAdmin` 可见，同样收进下拉。
  下拉内所有**禁用项**仅作分组标题使用，不参与命令分发。
- **「筛选方案」覆盖全部表格（含树表）**：原先只有走 `useTable` 的服务端分页列表页自带筛选方案，
  `menu` / `dept` 两个树表是**一次性拉全量、靠 computed 过滤**，拿不到该能力。现在
  `useTable` 抽出内部 `createFilterStore()`，新增导出 `useTableFilter()`（只做条件对象 +
  持久化，**不涉及分页与请求**，过滤结果由页面 computed 响应），两个树表改用它在工具栏出现
  「筛选方案」入口（筛选条件见下条：已统一改为列头筛选）。
  树形过滤规则：**命中节点保留整棵子树**；未命中但子孙命中的节点保留自身、`children` 换成过滤结果。
  `localStorage` 键沿用 `cccms_table_filter:` / `cccms_table_filter_scheme:`，与普通列表页一致。
- **筛选统一迁移到表格列头，移除全部顶部搜索区**：所有列表页（user / role / post / dept / menu /
  log / notice / online / crontab / tenant / data_rule / dict / file）不再使用 `<template #search>`
  搜索卡片，改为在列头筛选 —— `ArtTableColumn` 新增 `filter` 配置：
  - `type: 'enum'` 走 el-table 原生 `filters` **多选**勾选面板（`filter-method` 恒真，
    避免组件再对「当前页」做一次客户端过滤；真正的过滤在后端）；
  - `type: 'text'` 因 Element Plus 2.x 的 `el-table-column` **没有 filter 内容插槽**，
    改用 `#header` 插槽自绘「标题 + 漏斗图标 + 输入弹层」（回车 / 「确定」提交、「清除」清空）；
  - 筛选值统一经 `TABLE_FILTER_KEY` 的 `readFilter()` / `writeFilter()` 落到页面 `query`
    （多值以逗号串如 `1,0` 传递），因此**自动被「筛选方案」持久化**，服务端分页场景自动回第 1 页。
  - `ArtTable` 随之删除顶部搜索卡片、`#search` 插槽与 `search` / `reset` 两个 emit；
    页面删除 `@search` / `@reset` 绑定，并把所有筛选字段补进 `initialQuery`
    （它同时是筛选记忆回填的白名单）。
  - 后端新增 `support/FilterInput.php`（`values()` / `ints()`，兼容逗号串与数组并去重），
    12 个 Logic 的枚举筛选由单值 `where` 改为 `whereIn`；新增筛选维度：
    role 的 `data_scope`、notice 的 `level`、tenant 的 `name`/`code`/`contact`/`phone`、
    online 的 `username`/`ip`（`OnlineSession::paginate()` 改为按字段独立 AND 匹配）、dict 的 `type`；
    crontab 的 `group_name`、file 的 `ext` 由精确匹配改为模糊匹配。
- **表格列默认全部显示**：删除 `ArtTableColumn.defaultHidden` 字段与 `ArtTable` 的默认隐藏逻辑
  （`loadHidden()` / `resetColumns()` 回落到空数组），并移除 7 个页面里 11 处 `defaultHidden: true`。
  「列设置」popover 保留，用户仍可**手动**隐藏列。隐藏列的存储前缀升级为
  `cccms_table_hidden:v2:` —— 否则老用户浏览器里已存的隐藏记录会让改动「看起来没生效」。
- **列头筛选改为统一的「右侧漏斗 + 复选下拉框」弹层**：`type: 'enum'` 不再用 el-table 原生 `filters`
  （其面板底部是纯文本按钮 `<button>`、入口图标也只能跟在标题后面），与 `text` 统一走 `#header`
  插槽自绘弹层 —— 漏斗图标固定在**列头最右侧**（标题对齐跟随列的 `align`），有生效条件时高亮主题色；
  - 枚举列弹层内是 `el-select multiple` **复选下拉框**（折叠标签 + 可清空），多值仍按逗号拼接；
    该 `el-select` 必须 `:teleported="false"` —— 下拉面板默认挂到 `body` 会被 el-popover 的
    「点击外部即关闭」判成外部点击，勾第一个选项弹层就没了；
  - 弹层底部按规范**重置在左、筛选在右**，均为按钮形式（筛选为主题色 `primary`），点任一按钮后自动收起；
  - `ArtTable` 随之删除 `columnProps()` 里的 `filters` / `filterMultiple` / `filterMethod` /
    `filteredValue` / `columnKey` 与 `@filter-change`，`onColumnFilterChange()` 一并移除。
- **移除 `ArtTable` 内按 `filter` 自动生成的顶部搜索栏**：它是「筛选统一迁移到列头」时漏掉的残留，
  与列头筛选共用同一份 `query`、功能完全重复；`useTable` 的 `writeFilters()` / `resetFilters()`
  随之失去调用方一并删除，语言包清理 `query` / `expand` / `collapse` / `filterClear` 四个失效 key。
- **搜索框以「与列同源的独立搜索卡片」回归**：卡片放在表格卡片**上方**（`.art-table-search`，
  与表格卡片同级），搜索项由各列已有的 `filter` 配置**自动生成** —— 页面零接线、零重复声明，
  改列即改搜索项。仍然保留列头筛选，两者**共用同一份 `query`**，因此天然联动：
  列头改了顶部值跟着变，顶部查询后列头漏斗高亮，条件也照旧被「筛选方案」持久化。
  - 控件按 `filter.type` 生成：`text` → 输入框、`enum` → 多选下拉、`date` → 日期范围选择器；
    `queryKey` / `startKey` / `endKey` 与列头筛选一致（如 data_rule 的 `action`、crontab 的 `next_start`）。
  - 草稿与列头弹层分开保存，**输入过程不发请求**；点「查询」（或回车）用 `writeFilters()` 把全部字段
    一次性写回，只触发一次重新查询；「重置」清空全部筛选字段。
  - 搜索项超过 3 项（`SEARCH_COLLAPSE_LIMIT`）时默认折叠，右侧给「展开 / 收起」入口
    （新增语言包 key `table.searchExpand` / `table.searchCollapse`）。

### 移除

- **下线用户 / 数据权限规则的导入导出**：只保留**日志导出**（`GET /log/export`）。这三个能力
  使用频率低却带一整套模板下载、`el-upload` 直传与行级失败汇总，维护成本与收益不匹配。
  前端删除两个页面的导出 / 模板 / 导入按钮、导入 dialog 与相关 API 函数；后端删除
  `UserController` / `DataRuleController` 的 `export()` / `template()` / `import()`、
  `UserLogic` / `DataRuleLogic` 的对应实现与 CSV 常量、6 条路由与 6 个权限节点
  （`user:export|import|template`、`data_rule:export|import|template`）。
  `PermScanner::sync()` **只 upsert 不删除**，功能下线后老库里会留下点了也没用的僵尸按钮，
  因此 `cccms:db-upgrade` 新增幂等清理：查到残留才动手，先删 `sys_role_node` 授权记录再删
  `sys_menu` 行，并 `PermissionCache::bump()` 立即失效权限缓存。通用 `support/Csv.php`
  与前端 `downloadFile()` **保留**（日志导出仍在用）。

- **云存储凭证从 `.env` 迁到后台配置**（`upload.oss_*` / `upload.cos_*` / `upload.qiniu_*`）：
  原先驱动选择在 `sys_config.upload.storage_driver`、凭证却只能写在 `config/filesystem.php`
  （读 `OSS_*` / `COS_*` / `QINIU_*` 环境变量），一处配置分裂在两个地方，运维改凭证必须登服务器改 `.env`
  再重启。现在 `filesystem.php` 只留代码级默认值（初始驱动、本地 URL 前缀、白名单与大小上限），
  凭证全部落 `sys_config`：三个驱动各自从 `SysConfig` 读取，密钥类项（`*_secret` / `*_secret_key`）
  以 `enc:` + Cipher 密文存库、列表接口只回传「是否已配置」、留空提交即保持原值。
  配置页按当前 `upload.storage_driver` **动态显示该驱动的字段**（其余驱动字段隐藏但值保留，
  来回切换不会丢配置）。`.env.example` 与文档同步删除 `OSS_*` / `COS_*` / `QINIU_*`。
  升级：`cccms:db-upgrade` 幂等补插这 14 项配置（`INSERT IGNORE`，不覆盖已改过的值）。

### 性能

- **字典数据缓存（P2-8）**：新增 `support/DictCache.php`，复用 `PermissionCache` 的
  「全局版本号 + 缓存键含版本」范式（`cccms:dict:ver` + `cccms:dict:d:{ver}:{typeId}`，TTL 600）。
  `DictLogic::dataList()` 优先读缓存，字典增删改与**回收站还原 / 彻底删除**均 `bump()` 版本号；
  回收站视图直查库不缓存；Redis 不可用时自动降级直查库，功能不中断。

### 可观测性

- **日志链路聚合视图（P2-6）**：新增 `GET /log/trace?trace_id=` + 权限节点 `cccms:log:trace`，
  按 `trace_id` 把一次请求的全部日志串起来（按 `id` 升序 = 请求内时间顺序），并汇总
  `{total, failed, cost}`。前端日志页「链路ID」列可点 + 操作列「同链路」按钮 + 详情抽屉内
  「查看同链路」，以 `el-timeline` 时间线展示；支持 **URL 直达** `/log?trace_id=xxx`
  （从报错弹窗跳转即自动展开链路）。走同一份 `newScopedQuery()`，越权日志不出现。

### 体验

- **大表格虚拟滚动（P2-15）**：`ArtTable` 新增可选 `virtual` 属性（默认 `false`），开启后内部改用
  `el-auto-resizer` + `el-table-v2`，只渲染可视区域内的行。**业务页零改动**：`virtual` 仅在
  「非树形、非回收站」时生效，命中 `tree` / `recycle` 自动回退普通 `el-table`；列的 `slot`
  经 `cellRenderer` 重建（作用域仍是 `{ row, index, value }`），`selection` 用 `ElCheckbox`
  自绘并照常派发 `selection-change`，`fixed: 'right'` 映射为右固定列，分页 / 列设置 / 列宽记忆 /
  筛选方案与普通模式共用。列的 `width` 缺失时按 `minWidth` → `150px` 兜底。

### 修复

- **批量操作菜单项的按钮权限失效（P2-1 回归）**：用户 / 岗位 / 字典三个列表页的批量操作下拉菜单
  把 `v-auth` 直接挂在了 `el-dropdown-item` 上，而该组件根节点不是单个元素（内部是
  `ElRovingFocusItem`），运行时指令挂不上去 —— 控制台刷
  `Runtime directive used on component with non-element root node` 告警，且**没有对应权限的账号
  也能看到批量启停 / 删除 / 分配菜单项**（后端仍会拦，属界面越权暴露）。13 处改为
  `v-if="hasAuth('...')"` 条件渲染；`v-auth` 本身保留给 `el-button` / `el-dropdown` / `el-upload`
  等单根节点组件，并在 [05-鉴权与权限 §7](05-鉴权与权限.md#七前端按钮权限v-auth) 补上这条约束。
- **在线用户列表为空 / 总数与行数不符**：会话此前**只在登录那一刻登记**，`touch()` 与 `rotate()`
  又都刻意不补建，于是「令牌在本功能上线前签发」「登录登记时 Redis 抖动」「服务重启后仍持有效令牌」
  的用户会永远进不了在线列表 —— 明明在线，列表却是空的。现在 `OnlineSession::touch()` 在会话缺失时
  **按令牌声明自愈登记**（用 `iat` / `exp` 回填，展示的仍是真实登录时间），`CheckLogin` 传入用户上下文
  与声明即可。另外索引 ZSet 没有 TTL，过期成员一直留在里面被 `zCard` 算进总数（曾出现「共 2 条、只列 1 行」），
  现在列表查询会顺手摘掉读到的僵尸成员并从总数里扣除（`prune()` 仍作全量兜底）。

### 文档

- **修正文档过期与不一致（P1-8）**：权限节点总览移除已并入 `log` 的 `login_log` 模块并按实测重算为
  **95 个节点**；数据权限表格删除已 DROP 的 `sys_login_log` / `LoginLog` 行；
  `07-功能模块` 补齐章节编号断档（`十八 → 十九/二十/二十一`）与缺失的 `notice:options` 接口；
  `11-常用命令与运维` 补齐章节编号断档（`十 → 十一/十二`）与 Redis 键说明（新增
  `cccms:login_fail:ip:*` / `cccms:rate:*`）；`01-项目介绍` 删除「`cccms:update` 文档尚未补齐」的过期表述。
- **新增 [13-数据模型](13-数据模型.md)（P1-9）**：覆盖 `db/schema.sql` 全部 24 张表的分组、
  字段语义、枚举取值、索引意图与软删除 / 回收站约束；`docs/README.md` 目录已登记。
- **多租户（P2-17）文档同步**：[13-数据模型](13-数据模型.md) 补 `sys_tenant` 表结构与
  12 张表的 `tenant_id` / `idx_tenant` 约定；[06-数据权限](06-数据权限.md) 补
  「租户（硬边界）与数据档位（租户内软范围）」的关系、`$globalScope` 顺序原因、
  `withoutGlobalScope()` 保留租户 / `withoutAllScopes()` 才跨租户、
  直查必须走 `TenantContext::table()` / `stamp()`、全局唯一键查重**不**收敛租户。
- **「筛选方案覆盖全部表格 + 列默认全显 + 下线用户/规则导入导出」文档同步**：`01-项目介绍`
  的导入导出 / 前端体验两行改为「仅日志导出」与「筛选方案覆盖树表」；`03-目录结构`
  的 `useTable` 备注补 `useTableFilter`；`06-数据权限` 删除「十二、规则导入 / 导出」整节；
  `07-功能模块` 删除用户 / 数据权限规则的导入导出接口与说明；`09`/`10` 删除 `defaultHidden`
  与「导入用 `el-upload` 直传」约定；`12` 把「导入用户失败」改为「升级后残留权限节点清理」；
  `11` 的 `db-upgrade` 说明补清理动作；`开发计划` 的 P2-10 标记为明确不做。

### 工程化

- 新增测试用例：`PasswordPolicyTest`（密码策略 6 条）、`RateLimitTest`（限流 4 条）、
  `CorsTest`（CORS 白名单 5 条）、`StorageSecurityTest`（上传安全 2 条）、
  `TokenRenewalTest`（令牌滑动续期 4 条）、`I18nTest`（`has()` 命中、两语言包 key 集合一致、
  权限节点 slug 均有 `menu` 标题翻译，3 条），全量 **60 通过 0 失败**。
- 多租户（P2-17）改造后全量回归：后端 `php tests/run.php` **69 通过 0 失败**、
  `cccms:perm-scan --check`（132 个方法）校验通过、`cccms:data-scope-check` 接入检查通过、
  `cccms:db-upgrade` 重复执行为 **0 变更**（幂等）；前端 `vue-tsc --noEmit` / `lint` /
  `format:check` / `i18n:check`（两语言包 1036 key 严格对齐）/ `build` 全部通过。
- 前端新增 `npm run i18n:check`（`scripts/check-i18n.mjs`）：用 esbuild 编译语言包后比对
  `zh-CN` / `en-US` 的命名空间与 key 集合，**不一致直接失败**；另列出仍有硬编码中文的非注释行
  仅作提示。已接入 `frontend` CI 门禁（`format:check` → `lint` → `i18n:check` → `build`）。

### 待处理 · 文档同步（并行开发中的功能）

- [x] **上游代码同步（`cccms:update`）已写入文档**：命令用法 / 参数 / 退出码见
      [11-常用命令与运维 §1.7](11-常用命令与运维.md#17-cccmsupdate--从上游仓库同步框架更新)，
      升级流程见 [02-安装部署 §6.1](02-安装部署.md#61-从上游同步框架代码)，
      新增文件登记见 [03-目录结构](03-目录结构.md)（`config/upgrade.php`、`support/Upgrader.php`、
      `command/UpgradeCommand.php`、`command/task/UpgradeCheckTask.php`）。

### 待处理 · 后续

- [x] **i18n 批次二（P2-16 续）已完成**：其余 7 个页面
      （`data_rule` / `notice` / `crontab` / `file` / `upgrade` / `generator` / `dashboard`）
      与公共组件（`CronEditor.vue` / `ArtNodePicker.vue`）均已迁移，详见上方「新增 → P2-16」。
      `npm run i18n:check` 现仅以提示形式列出剩余硬编码中文行（**均为注释与用户录入数据**）。
- [x] **多租户（P2-17）已完成**：单库共享表 + `tenant_id`（`0` = 平台租户）的租户隔离已落地，
      租户作为最外层硬边界叠加在数据权限之上，超管显式切换租户（JWT 带 `tid` 并重签），
      详见上方「新增 → 多租户（P2-17）」与
      [开发计划 §二 · P2-17](开发计划.md#p2-17-多租户设计口径与隔离边界)。

### 明确不做

- [~] **前端 E2E（Playwright）**：团队不接受用例随 UI 变动长期维护的成本；
      主链路回归改由后端集成测试（鉴权矩阵 / 数据权限三档 / 回收站）+ 前端
      `format:check` / `lint` / `build` 覆盖。如后续引入，再单独立项。
- [~] **多因素认证 TOTP（P2-4）**：当前安全基线已由「口令策略 + 登录限流 + 图形验证码 +
      令牌短 TTL（2h）+ 滑动续期」覆盖，TOTP 的收益与改造量不成正比，暂不引入。

---

## [2.1.0] - 2026-09-18 · 交付质量补齐

### 新增

- **国际化（i18n）**：后端 `support/I18n.php` + `lang/{zh-CN,en-US}/*.php`，locale 解析优先级
  `?lang=` → `Accept-Language` → 配置默认（`plugin.cccms.app.locale`，默认 `zh-CN`）；
  前端 `vue-i18n` + `src/locales/`，`ElConfigProvider` 与设置项响应式联动，
  `api/request.ts` 自动携带 `Accept-Language`。
- **通知公告定向投放**：新增 `sys_notice_target` 关系表，发布范围支持「全部 / 按部门 / 按角色 /
  指定用户」，阅读侧按同一口径过滤（`NoticeLogic::targetedNoticeIds()`）。
- **通知公告已读回执报表**：`NoticeLogic::readReport()` + 管理端「回执」抽屉
  （应读人数 / 已读人数 / 已读明细，口径与推送侧一致）。
- **数据权限规则导入 / 导出**：`DataRuleLogic::export()/template()/import()` + 三个路由
  （`/data_rule/export`、`/data_rule/template`、`/data_rule/import`）；CSV 复用通用导入导出能力，
  支持「新增 / 更新」与行级失败汇总。
- **前端路由级权限守卫**：`router.beforeEach` 在动态路由注册完成后按 `meta.node` 校验，
  无权跳新增的 `/403` 页（`pages/cccms/error/403.vue`，刻意不带 `meta.node` 以防无限重定向）。
- **列表页筛选方案保存**：`useTable` 新增 `listSchemes / saveScheme / applyScheme / removeScheme`，
  当前条件自动回填（`localStorage['cccms_table_filter:<key>']`），命名方案存
  `cccms_table_filter_scheme:<key>`，入口在 `ArtTable` 工具栏。
- **日志归档到对象存储**：`cccms:log-archive` 命令 + `LogArchiver`（**先上传成功再按 id 删除**）+
  `LogRedactor`（与操作日志中间件共用同一份脱敏规则）+ `config/log.php` 的 `archive` 段；
  内置任务「日志归档到对象存储」**默认停用**，确认对象存储可用后再启用。
- **存储驱动 `put()` / `objectPath()`**：支持指定路径写入，并防目录穿越
  （local / OSS / COS / 七牛统一实现）。

### 变更

- **角色数据档位不继承父角色（定案）**：档位只取「直连角色」中最宽松的一档（`min`），
  祖先角色的档位不参与；有效角色（直连 + 祖先）仅用于「规则绑定角色」与 `{role.ids}`。
  避免父角色的「全部数据」把子角色**隐式扩权**。
- **`sys_role.data_scope` 默认值 `1`（全部数据）→ `4`（仅本人）**：档位靠「取最宽松」生效，
  漏配时必须是**最窄**兜底，否则新建角色会静默拿到全库可见范围（fail-closed 兜底）。
  `cccms:db-upgrade` 新增「列默认值纠偏」能力：只改默认值、不动已有行数据。
- **登录日志区分口径由 `type` 改为 `path`**：`sys_log` 去掉 `type` 列与 `idx_type` 索引，
  登录记录固定 `path='/auth/login'`（操作日志中间件不记该路径，二者天然互斥）；
  日志列表的「类型」筛选换成「请求路径」筛选，导出列同步调整。
- **ESLint 9 → 10**：`eslint@10` / `@eslint/js@10` / `eslint-plugin-vue@10` /
  `vue-eslint-parser@10`，仍为扁平配置（格式仍交给 Prettier）。
- **在线用户服务端分页**：`OnlineSession::entries()` 改为分段扫描（`SCAN_BATCH = 200`）+
  服务端过滤，去掉单次最多 2000 条索引成员的上限。
- **强下线黑名单 TTL 精确化**：会话 Hash 补存令牌 `exp`，`kick()` 按真实剩余有效期写黑名单。
- **用户导入支持角色 / 部门 / 岗位**列（编码 / 多值列约定见 [07](07-功能模块.md#四用户管理user)）。
- **附件在线预览**：`FileLogic` 出参新增 `is_pdf`，PDF 用内嵌 iframe 预览（图片沿用原预览器）。
- **定时任务失败重试独立成表**（`sys_crontab_retry`），不再「吃掉」该秒的正常调度。
- **慢接口告警**：`OperationLog::alertSlow()` + `log.slow_threshold` 配置项。

### 修复

- **`data-scope-check` 6 处违规**：`LogLogic::recordLogin()` 与 `NoticeLogic` 的 5 处查询原先直连
  `Db`，改为模型 + 显式 `withoutGlobalScope()`，并逐处写明「为什么必须看全量」。
- **前端 `format:check` 失败**：`api/upgrade.ts`、`pages/cccms/upgrade/index.vue` 重新格式化。

### 移除

- **`sys_login_log` 表与 `LoginLog` / `LoginLogLogic`**：登录记录统一并入 `sys_log`
  （`path = '/auth/login'`），由 `LogLogic::recordLogin()` 承担；`cccms:db-upgrade` 会
  **DROP `sys_login_log`** 并清掉 `login_log` 权限节点，「系统设置 → 登录日志」页面随之下线。

### 工程化

- **集成测试**：`tests/cases/AuthMatrixTest.php`、`DataScopeIntegrationTest.php`、
  `RecycleIntegrationTest.php` 纳入 CI。
- **`cccms:db-upgrade` 支持列默认值纠偏**（见上「变更」）。
- **文档同步**：`开发计划`（3.2 清零 / 3.3 定案 / M5 收口）、`01-项目介绍`、`03-目录结构`、
  `05-鉴权与权限`、`06-数据权限`、`07-功能模块`、`09-前端开发规范`、`10-前端组件参考`、
  `11-常用命令与运维`、`12-常见问题与排错`。

---

## [2.0.0] - 2026-09-18 · 文档体系 + 能力补齐 + 工程化

### 新增

- **在线用户 / 强制下线**：`support/OnlineSession.php`（Redis 会话索引：`cccms:online:s:{jti}` +
  `cccms:online:index`）、`OnlineLogic` / `OnlineController`、页面「系统设置 → 在线用户」。
  支持按会话或按用户强制下线（同时作废已发出的令牌）。
- **登录日志**：`sys_login_log` 表 + `LoginLog` 模型 + `LoginLogLogic`（登录成功 / 失败都记录）+
  页面「系统设置 → 登录日志」，支持筛选、导出、批量删除与清空。
  （**2.1.0 已移除**：登录记录并入 `sys_log`，详见下方 2.1.0「移除」。）
- **通知公告**：`sys_notice` / `sys_notice_read` 表 + `Notice` / `NoticeRead` 模型 + `NoticeLogic`，
  管理页面「系统设置 → 通知公告」；顶栏「我的消息」抽屉 + 未读角标（`stores/notice.ts`）。
- **通用导入 / 导出（CSV）**：`support/Csv.php`（UTF-8 BOM、RFC 4180 转义、**公式注入防护**、
  导入行数与大小上限）；用户支持导出 / 模板下载 / 导入，操作日志与登录日志支持导出。
- **定时任务增强**：`sys_crontab` 新增 `group_name` / `overlap` / `timeout` /
  `retry_times` / `retry_interval` / `retry_left` / `retry_at` / `running` / `running_at`；
  实现**重叠保护**（默认跳过）、**超时释放运行锁并留痕**、**失败非阻塞重试**、**任务分组**；
  `sys_crontab_log` 新增 `source`（cron / retry / manual / timeout），状态扩展为
  1 成功 / 0 失败 / 2 跳过 / 3 超时释放。
- **请求链路 ID（traceId）**：由 `Cors`（中间件链第一环）生成，写入 `sys_log.trace_id`、
  回写响应头 `X-Trace-Id`，失败响应体也附带 `trace_id`；操作日志页面可按键检索。
- **权限集合缓存**：`support/PermissionCache.php`（Redis + **全局版本号失效**），
  `AuthService::buildContext()` 改为「用户行实时查 + 角色/权限走缓存」，
  RBAC 变更处统一调用 `bump()`（角色、授权节点、菜单、用户角色关系、回收站、菜单/权限同步、清理缓存）。
- **令牌失效名单**：`support/TokenBlacklist.php`，支持**单令牌黑名单**（按 `jti`）与
  **用户级时间分界线**（强制下线）。
- **后端测试运行器**：`tests/run.php`（零依赖，支持 `--filter`、数据库不可用自动跳过），
  用例覆盖 Cron 匹配、权限节点规范、CSV、数据规则体检；`composer test` 一行运行。
- **CI**：`.github/workflows/php.yml`（MySQL + Redis 服务、初始化库、三个检查命令、单测）与
  `.github/workflows/frontend.yml`（`format:check` → `lint` → `build`）。
- **环境变量示例**：`server/.env.example`，集中说明应用 / 数据库 / Redis / 鉴权 / 存储变量；
  Redis 配置支持 `REDIS_HOST` / `REDIS_PORT` / `REDIS_PASSWORD` / `REDIS_DB` 覆盖。
- **文档体系**：新增 `docs/`（导航与编写规范 + 12 篇正文 + 本更新日志 + 开发计划），
  `README.md` 重写为 GitHub 风格项目主页。
- **前端**：顶栏**快捷导航**（Ctrl/⌘ + K 搜索菜单跳转）、**面包屑可点击**、
  **我的消息抽屉**、`ArtTable` **列宽拖拽记忆**、用户导入导出按钮、日志链路 ID 检索与导出。

### 变更

- **登出改为服务端失效**：`AuthLogic::logout()` 现在把当前 `jti` 写入 Redis 黑名单
  （TTL = 剩余有效期）并移除在线会话；`CheckLogin` 每次校验黑名单。
- **`DemoTask` → `LogCleanTask`**：语义明确的生产任务（清理历史操作日志），
  并在 `seed.sql` 预置「每天 03:00 执行」的初始任务（原先需手工创建）。
- **`debug` 改为环境变量控制**：`server/config/app.php` 的 `debug` 读取 `APP_DEBUG`，
  **默认 `false`**（安全默认）；本地开发在 `.env` 中设 `APP_DEBUG=true`。
- **数据权限角色口径统一**：规则的「绑定角色」与动态变量 `{role.ids}` 改用
  **有效角色（直连 + 祖先）**，与鉴权同口径；角色**档位** `data_scope` 仍只按直连角色取最宽松值
  （避免子角色因父角色隐式扩权）。
- **受控表默认登记**：`seed.sql` 补登记 `dept`，与开发库及文档保持一致。
- **前端代码风格统一**：引入 Prettier 并全量格式化；`env.d.ts`、`CronEditor` 的
  两处写法按 ESLint 规则收敛。
- **`UserContext::fromClaims()` 移除**：骨架期遗留、无调用方，实际链路为 `AuthService::buildContext()`。

### 修复

- **`support/DataScope.php` 类注释与实现矛盾**：注释曾写「5 自定义规则……没有规则 = 等同全部」，
  但实现是 `whereRaw('1 = 0')`（fail-closed）。注释已订正，并补充「角色绑定 vs 角色档位」的口径说明。
- **`db/seed.sql` 注释过期**：原写「配置项尚未被后端业务代码消费」，实际各配置项已全部接入，已订正。
- **`Dockerfile` 命令拼接缺陷**：`RUN docker-php-source delete \` 换行接 `rm -rf ...`，
  反斜杠续行会把两条命令并成一条，`rm` 被当作参数而**不会执行**（apk 缓存未清理）。已重写。
- **Docker 部署不完整**：镜像原先不 COPY 代码、不含 MySQL / Redis。已改为
  多阶段式构建（依赖层缓存 + `composer install --no-dev`）、可选安装 redis 扩展、
  带健康检查，`docker-compose.yml` 补齐 webman + mysql + redis 与自动初始化脚本。
- **`Result::currentEncode()` / `traceId` 在 CLI 下的空指针风险**：统一走 `function_exists('request')` 判断。

### 文档

- 新增 `docs/01` ~ `docs/12`、`docs/README.md`（含文档编写规范）、`docs/开发计划.md`、本文件。
- `README.md` 重写：徽章、简介、功能特性、技术栈、目录结构、快速开始、安全设计要点、
  文档索引、常用命令、贡献、协议、致谢。
- 全部文档同步了本轮变更（新增模块、命令、环境变量、Redis 键、CI、约定）。

### 工程化

- `.dockerignore` 新增；`Dockerfile` / `docker-compose.yml` 重写。
- `composer.json` 增加 `test` 脚本。
- `frontend/package.json` 增加 `lint` / `lint:fix` / `format` / `format:check`，新增
  `eslint.config.js`（扁平配置）、`.prettierrc.json`、`.prettierignore`。

---

## [1.2.0] - 2026-09-17 · 数据权限下沉到模型层

> 这是本项目规模最大的一次重构：把数据权限从「Logic 层显式调用」改为「模型层自动生效」。

### 新增

- **`BaseModel` 全局查询作用域**：`protected $globalScope = ['dataScope']` +
  `scopeDataScope()` + `$dataScope` 声明位 + `newScopedQuery()`。
- **请求级 scope plan**：`DataScope` 用 `WeakMap`（键为 `UserContext` **实例**）缓存
  角色 / 岗位 / 部门 / 规则 / 受控表，一次请求只解析一次。
  实测：同请求 10 次模型查询 → 数据权限相关 SQL **0 条**（无缓存对照 50 条）。
- **写入侧字段级防护**：`app/model/ScopedQuery.php`（`BaseModel::$query`），
  在 `insert` / `update` / `save` / `insertAll` 前剔除 `hidden` / `readonly` / `mask` / `encrypt` 字段。
- **检查命令 `cccms:data-scope-check`**（`DataScopeChecker`）：
  R1 控制器不得直连数据库；R2 受控表必须有模型且不得声明 `$dataScope = false`；
  自动发现业务插件（以 `db/menu.php` 判定）；首轮即抓出 10 处真实漏网。
- **规则体检 `cccms:data-rule-check`**（`RuleConflict`）：条件互斥 / 同字段多动作 /
  恒不生效 / 表未受控；只报「可证明为空」的组合（宁漏不误）。
- **规则「绑定关系：或 / 且」**：`sys_data_rule.bind_mode`（`or` 默认 / `and`）；
  未填写的维度不参与判断。
- **受控表管理**：`sys_data_scope_table` + 管理界面（哪些表可配**自定义规则**）。

### 变更

- **数据权限语义收紧**：自定义档（`data_scope = 5`）的 fail-closed **无条件成立** ——
  没有规则 / 一条都没命中 = 看不到任何数据。
- **软删除下沉到模型层**：`BaseModel` 使用 think-orm 的 `SoftDelete` trait，
  查询自动排除已删数据；`support/SoftDelete.php` 职责收窄为「查询构造器直查」场景。
- **Logic 层迁移**：用户 / 角色 / 部门 / 岗位 / 附件 / 定时任务 / 操作日志 / 数据权限规则
  共 8 个 Logic 的 `Db::name()` 清零（`information_schema` 反射除外）。
- **`dept` 登记受控表**：可配自定义规则；登记只影响「能否配自定义规则」，
  预设基线由模型声明决定，两者解耦。

### 修复

- **回收站恢复时把待恢复行自己算作占用**：导致 user / role / post / dict_type / menu
  根本无法恢复（查重未排除已删行）。
- **`DashboardLogic` 口径泄漏**：附件 / 日志列表已按数据范围收窄，工作台却仍显示全量总数；
  现全部改为模型查询，随范围聚合（超管 `user=2 dept=2 log=33` → 仅本人档 `user=1 dept=0 log=0`）。
- **`dept` 登记后被检查命令抓出的漏网**：`ProfileLogic` 仍在用 `Db::name('dept')`，已迁移。
- **数据规则「排序」字段是个假旋钮**：运行时完全不读 `sort`，但字段级动作是**按顺序依次处理**的，
  原先不指定顺序导致结果不可复现；现固定 `ORDER BY id ASC`，并移除该字段。

### 移除

- Logic 层的 `DataScope::field()` / `stripRow()` / `stripProtected()` 调用与旧方法（改由模型层承担）。
- `sys_data_rule.sort` 列（不影响规则生效，界面上也不展示）。

---

## [1.1.0] - 2026-09 · 二期（P1）

### 新增

- **定时任务**：`sys_crontab` / `sys_crontab_log`、六段 Cron（`CronMatcher`）、
  每秒调度进程（`process/Crontab.php`）、执行白名单（`CrontabTask` 接口，杜绝 RCE）、
  执行日志、立即执行；前端宝塔式 `CronEditor`。
- **代码生成器**：按库表生成 Model / Logic / Controller / 路由 / 前端 API / 前端页面 6 类产物，
  自动登记菜单；生成即符合 fail-closed。
- **多存储驱动**：`StorageManager` + `StorageDriver` 抽象 + local / OSS / COS / 七牛。
- **前台应用插件**：独立 `plugin/index`（公开路由 `/site/*`，与后台中间件隔离）。

---

## [1.0.0] - 2026-09 · 一期（P0）

### 新增

- Webman 插件化骨架（`plugin/cccms`）、`app/` 主应用、全局配置。
- **鉴权体系**：4 个 PHP 8 原生属性（`Permission` / `NoAuth` / `NoLogin` / `Restrict`）+
  中间件链（`Cors → Xss → CheckLogin → Maintenance → CheckAuth → ResponseEncode → OperationLog`）+
  `cccms:perm-scan` 校验命令；**fail-closed**（未声明注解直接拒绝）。
- **RBAC**：用户 / 角色（含继承）/ 菜单（节点）管理；用户 ↔ 角色、角色 ↔ 节点。
- **组织架构**：部门（无限级、用户可多部门）/ 岗位；用户 ↔ 部门、部门 ↔ 角色、用户 ↔ 岗位。
- **统一响应**：`Result` 支持 `json / jsonp / xml / view`，`ExceptionHandler` 统一异常渲染。
- **菜单体系**：`db/menu.php` 声明目录 / 菜单 + `cccms:menu-sync`；按钮由注解生成。
- **系统维护**：顶栏「同步 / 清理缓存」，与 CLI 共用同一份实现。
- **个人中心**：静态路由 `/profile`，入口在右上角用户下拉。
- **回收站**：业务主数据软删除；各模块表格右上角切换查看已删数据。
- **系统配置**：`sys_config` 键值配置 + 可视化表单（5 组，项数随版本增长，当前 32 项）。
- **数据字典**：类型 + 数据两级结构，共用通用分类 `sys_category`。
- **操作日志**：记录请求参数、执行结果、耗时、IP、UA，敏感字段递归脱敏。
- **附件管理**：本地驱动 `public/storage`，URL 统一由 `url_prefix` 生成。
- **基础防护**：全局 XSS 过滤、CORS、统一异常响应、登录失败限流、图形验证码、密码策略。

---

## 附录 A · 数据权限迁移决策与验证记录

> 以下为 `[1.2.0]` 迁移过程中的决策与实测数据，保留作为设计依据（由原 `待办.md` 整理）。

### A.1 三个必须区分开的概念

| 概念 | 含义 | 决定方 |
|---|---|---|
| **模型化** | 该表查询改走 `Model` 而不是 `Db::name()` | 各模块迁移进度 |
| **参与数据权限** | 是否走预设基线（仅本人 / 本部门） | 模型的 `$dataScope` 声明 |
| **登记受控** | 是否可配**自定义**行级 / 字段级规则 | `sys_data_scope_table` |

### A.2 三条设计铁律

1. **表名以模型为准**：`applyToModelQuery()` 强制用 `$model->getName()` 覆盖声明里的表名。
2. **用户上下文只从 `request()->user` 取**，不新建静态容器（非 fiber 环境下
   `Workerman\Coroutine\Context\Fiber` 会把数据写进 worker 级全局，导致跨请求身份泄漏）。
3. **无用户 ⇒ 不作用域**（fail-open，但边界封闭）：只剩登录链路、CLI、`#[NoLogin]` 三类场景，
   且 `CheckLogin` / `CheckAuth` 会先对受保护路由抛 401。

### A.3 表 → 语义 → 状态

| 功能模块 | 表 | 模型 | owner（仅本人） | dept（本部门） | 模型声明 | 登记受控 |
|---|---|---|---|---|---|---|
| 用户管理 | `sys_user` | `User` | `id` | 经 `sys_user_dept` | 参与 | 已登记 |
| 部门管理 | `sys_dept` | `Dept` | 我所属部门 | 部门子树（`id IN`） | 参与 | 已登记 |
| 附件管理 | `sys_file` | `File` | `create_by` | 经 `create_by` → `sys_user_dept` | 参与 | 已登记 |
| 定时任务 | `sys_crontab` | `Crontab` | — | — | 参与（`no_baseline`） | 已登记 |
| 操作日志 | `sys_log` | `OperationLog` | `user_id` | 可见部门成员 | 参与 | 已登记 |
| 角色管理 | `sys_role` | `Role` | — | — | 不参与（已定） | 不登记（已定） |
| 岗位管理 | `sys_post` | `Post` | — | — | 不参与 | 不登记 |
| 数据权限规则 | `sys_data_rule` | `DataRule` | — | — | 不参与（已定） | 不登记（已定） |
| 通知公告 | `sys_notice` | `Notice` | — | — | 不参与（广播内容无归属） | 不登记 |

**明确不纳入范围**：`sys_menu` / `sys_role_node`（权限节点树，与数据权限正交）、
`sys_config`、`sys_dict_*`、`sys_category`、`sys_data_scope_table`、
`sys_crontab_log`、全部关联表（`sys_user_role` / `sys_user_dept` / `sys_user_post` / `sys_dept_role`）。

### A.4 绑定语义实测（`or` / `and`）

规则统一把 `user` 表收窄成 `id = 1`，命中即可见 1 行：

| 场景 | `or` | `and` |
|---|---|---|
| 无规则 | 0 | 0 |
| 只绑「自己有的岗位 A」 | 1 | 1（未填项不参与） |
| 「别人」+「自己的岗位 A」 | **1** | **0** |
| 「自己」+「自己的岗位 A」 | — | 1 |
| 「自己」+岗位 A + 部门 #1（三项） | — | 1 |
| 四项全绑、角色绑错 | — | **0** |
| 四项都不填（全局） | 1 | 1 |
| `bind_mode = 'AND'`（大写） | — | 按 `and` 生效 |
| `bind_mode = 'xxx'`（非法） | — | 兜底按 `or` |

### A.5 端到端验证矩阵（`[1.2.0]`）

| 场景 | 结果 |
|---|---|
| 超管 | `SELECT * FROM sys_user`（无范围条件）✓ |
| 仅本人 | `User` → `WHERE id = '990099'`；`File` → `WHERE create_by`；`Dept` → 我所属部门 ✓ |
| 仅本人 | `Role` / `Menu` → 无任何条件（声明不参与）✓ |
| 本部门及以下 | `Dept` → `WHERE id IN ('1','2')`（子树展开）✓ |
| 全部数据 / 无用户 | 无范围条件 ✓ |
| 逃生口 | `User::withoutGlobalScope()` → 无条件 ✓ |
| 越权拦截 | `read` / `update` / `delete` / `resetPassword` 对他人 id 全部 403 ✓ |
| 唯一性校验 | 仍能看全量（重名被正确拒绝）✓ |
| 请求级缓存 | 同请求 10 次模型查询 → 权限 SQL 0 条；无缓存对照 50 条 ✓ |
| 部门树作用域过滤 | 可见节点父不在集合时**提升为顶层**，否则整树为空 ✓ |
| 字段级规则 | 出参 `phone = 138****0000`；`update` 提交掩码 / 只读字段**均未写入** ✓ |
| 软删除 | 列表排除 / 回收站包含；未加 `withTrashed()` 的更新实测 0 行（陷阱确认）✓ |

### A.6 关于 `ks_*` 表（已定案）

`sys_data_scope_table` 里的 6 张 `ks_*` 表（`ks_subject` / `ks_account` / `ks_asset` /
`ks_rule` / `ks_alert` / `ks_batch_job`）属于**下游业务插件系统**。
本仓库是基础系统、不负责它们的 Logic；检查命令对「主代码中找不到模型的受控表」
只报 `NOTICE`，不计入错误，CI 不会被它们卡住。

---

## 附录 B · 维护约定

- 新增功能 / 修复缺陷时，**先在本文件对应版本下补条目，再改代码**（或提交后立即补）。
- 版本号：破坏性变更 → 主版本；新增能力 → 次版本；修复与文档 → 修订号。
- 未落地的条目放在 `[未发布]`，完成后移动到具体版本并勾选。
- 机制、接口、命令、配置或目录结构变化时，**同步更新 `docs/` 下对应文档**（见
  [`docs/README.md`](README.md) §三 文档编写规范）。
