# 目录结构

> 本文给出仓库整体与前后端的目录职责、命名约定。看懂这一篇，找代码基本靠直觉。

---

## 一、仓库顶层

```
cccms/
├── .github/         # CI 工作流（后端 / 前端）
├── docs/            # 项目文档中心（含更新日志与开发计划）
├── frontend/        # 管理前端（Vue 3）
├── server/          # 后端服务（Webman）
└── README.md        # 项目主页（面向 GitHub 首页）
```

---

## 二、后端 `server/`

```
server/
├── app/                          # 主应用（框架骨架，几乎不放业务）
│   ├── controller/               # 预留（空）
│   ├── middleware/StaticFile.php
│   ├── model/Test.php
│   ├── process/                  # Http.php（HTTP 进程）、Monitor.php（文件监控热重载）
│   ├── view/index/view.html
│   └── functions.php             # 自定义全局函数
│
├── config/                       # 全局配置（Webman 标准配置）
│   ├── app.php                   # debug、时区、路径、控制器后缀
│   ├── server.php                # 监听地址与端口（默认 0.0.0.0:8787）
│   ├── process.php               # 进程（webserver / monitor）
│   ├── middleware.php            # 全局中间件（本仓库为空，鉴权中间件由插件注册）
│   ├── route.php                 # 全局路由 + 404 兜底（统一 JSON + 记录 route-404.log）
│   ├── redis.php                 # Redis 客户端（phpredis / predis 自动回退）
│   ├── static.php / log.php / session.php / view.php / translation.php
│   ├── container.php / dependence.php / autoload.php / bootstrap.php / exception.php
│   └── plugin/webman/console/app.php
│
├── plugin/
│   ├── cccms/                    # ★ 后台核心插件（框架级基础功能，禁止塞业务）
│   │   ├── app/
│   │   │   ├── controller/       # 21 个控制器（含 Online / Notice / Upgrade）
│   │   │   ├── middleware/       # Cors / Xss / CheckLogin / Maintenance / CheckAuth / ResponseEncode / OperationLog
│   │   │   ├── model/            # BaseModel + ScopedQuery + 23 个业务模型
│   │   │   └── logic/            # 22 个 Logic（业务逻辑层，DB 操作收口）
│   │   ├── basic/
│   │   │   └── BaseController.php    # ok() / fail() 响应便捷方法
│   │   ├── command/
│   │   │   ├── MenuSyncCommand.php       # cccms:menu-sync
│   │   │   ├── PermScanCommand.php       # cccms:perm-scan
│   │   │   ├── DbUpgradeCommand.php      # cccms:db-upgrade
│   │   │   ├── DataScopeCheckCommand.php # cccms:data-scope-check
│   │   │   ├── DataRuleCheckCommand.php  # cccms:data-rule-check
│   │   │   ├── LogArchiveCommand.php     # cccms:log-archive
│   │   │   └── task/                     # 定时任务：LogCleanTask（清理日志）/ LogArchiveTask（归档到对象存储）
│   │   ├── config/               # 插件配置（Webman 按插件目录自动加载）
│   │   │   ├── app.php           # enable / locale / locales
│   │   │   ├── auth.php          # JWT 密钥/ttl、字段加密密钥
│   │   │   ├── database.php      # think-orm 连接（前缀 sys_）
│   │   │   ├── filesystem.php    # 存储驱动与四家云存储凭证
│   │   │   ├── log.php           # 日志归档（天数 / 批量 / 驱动 / 路径）
│   │   │   ├── middleware.php    # 插件中间件链（顺序即执行顺序）
│   │   │   ├── process.php       # 定时任务调度进程
│   │   │   ├── response.php      # 默认允许的响应编码
│   │   │   ├── route.php         # 全部后台接口路由（显式声明）
│   │   │   ├── exception.php     # 插件异常处理器
│   │   │   └── bootstrap.php     # DatabaseBootstrap（注入 ORM 配置）
│   │   ├── db/
│   │   │   ├── schema.sql        # 核心表结构
│   │   │   ├── seed.sql          # 初始化数据（可重复执行）
│   │   │   └── menu.php          # 目录 / 菜单声明（按钮由注解生成）
│   │   ├── lang/                 # 后端语言包（zh-CN / en-US）
│   │   ├── process/Crontab.php   # 定时任务调度进程（每秒 tick + 失败重试）
│   │   └── support/              # 框架核心（45 个 PHP 文件）
│   │       ├── attribute/        # Permission / NoAuth / NoLogin / Restrict
│   │       ├── storage/          # StorageDriver + Local/Oss/Cos/Qiniu + StorageManager
│   │       ├── Result.php / ApiException.php / ExceptionHandler.php
│   │       ├── PermissionMeta.php / ControllerScanner.php / PermScanner.php / MenuSyncer.php
│   │       ├── AuthService.php / PermissionCache.php / TokenService.php / TokenBlacklist.php
│   │       ├── UserContext.php / SessionGuard.php / OnlineSession.php
│   │       ├── LoginThrottle.php / PasswordPolicy.php / Captcha.php
│   │       ├── DataScope.php / DataScopeChecker.php / RuleConflict.php
│   │       ├── SoftDelete.php / SysConfig.php / Cipher.php / Csv.php
│   │       ├── CrontabTask.php / CrontabRunner.php / CronMatcher.php
│   │       ├── I18n.php / LogArchiver.php / LogRedactor.php
│   │       ├── FileStorage.php / Database.php / DatabaseBootstrap.php / SqlFileRunner.php
│   │
│   └── index/                    # 前台公开应用插件（与后台中间件隔离）
│       ├── app/controller/HomeController.php
│       └── config/               # app.php / route.php / middleware.php / exception.php
│
├── public/                       # 站点根目录
│   ├── favicon.ico
│   └── storage/                  # 本地附件目录（运行时创建，禁止解析 PHP）
│
├── tests/                        # 后端测试（零依赖运行器 + 用例）
│   ├── run.php                   # 入口：php tests/run.php [过滤词]
│   └── cases/                    # 用例：CronMatcher / 权限节点规范 / Csv / 规则体检
│                                 # 集成：AuthMatrixTest / DataScopeIntegrationTest / RecycleIntegrationTest
│
├── runtime/                      # 运行时（日志 / pid / 状态，不纳入版本控制）
├── support/                      # Webman 框架自带（Request / Response / Setup）
├── Dockerfile / .dockerignore / docker-compose.yml
├── .env.example                  # 环境变量示例（复制为 .env）
├── composer.json
├── webman                        # Webman 控制台入口
├── start.php                     # Linux / macOS 启动入口
├── windows.php / windows.bat     # Windows 启动入口
└── .gitignore
```

根目录另有 CI 工作流：

```
.github/workflows/
├── php.yml          # 后端：MySQL + Redis 服务 → 初始化库 → 三个检查命令 → composer test
└── frontend.yml     # 前端：format:check → lint → build
```

> **说明**：`server/config/plugin/` 下只有 `webman/console/app.php`（框架控制台配置）。**插件自身的配置在 `server/plugin/{插件}/config/`**，两处不要混淆。

---

## 三、前端 `frontend/`

```
frontend/
├── public/
│   └── logo.png                  # 内置 Logo（后台未配置 system.logo 时使用）
├── src/
│   ├── api/                      # 接口封装（一个后端模块一个文件）
│   │   ├── request.ts            # ★ axios 单例：鉴权头 / 错误提示 / 401 跳转 / 响应解包 / 文件下载
│   │   └── auth.ts user.ts role.ts menu.ts dept.ts post.ts dict.ts category.ts
│   │       config.ts dataRule.ts file.ts log.ts online.ts notice.ts
│   │       crontab.ts dashboard.ts generator.ts profile.ts system.ts recycle.ts
│   │       upgrade.ts types.ts
│   ├── assets/styles/            # 样式
│   │   ├── index.css             # 入口：Tailwind + token 映射
│   │   ├── tokens.css            # ★ 设计 token 唯一来源（--art-*）
│   │   ├── element.css           # Element Plus 主题覆盖
│   │   └── app.css               # 业务全局（高度链 / 过渡 / 进度条）
│   ├── components/core/          # 通用业务组件
│   │   ├── ArtTable.vue          # 列表页核心表格（搜索/工具栏/分页/回收站）
│   │   ├── ArtSplitView.vue      # 左右分栏
│   │   ├── ArtTreePanel.vue      # 左侧树面板
│   │   ├── ArtIcon.vue           # 按名字渲染图标
│   │   ├── ArtNodePicker.vue     # 节点选择器（树 / 远程搜索两种模式）
│   │   ├── ArtSettingsDrawer.vue # 主题设置抽屉
│   │   ├── CronEditor.vue        # Cron 可视化编辑器（六段）
│   │   └── RecycleToggle.vue     # 回收站开关
│   ├── composables/
│   │   ├── useTable.ts           # 列表页通用逻辑（分页 / 查询 / 加载 / 筛选方案）
│   │   └── useRecycle.ts         # 回收站开关 + 还原 / 彻底删除
│   ├── directives/index.ts       # v-auth 按钮级权限指令
│   ├── layouts/
│   │   ├── BasicLayout.vue       # 主布局
│   │   └── components/           # LayoutHeader / LayoutSidebar / LayoutTagbar / SidebarSubmenu / LayoutContent
│   ├── locales/                  # i18n 语言包（index.ts / zh-CN.ts / en-US.ts）
│   ├── pages/cccms/              # 页面（按插件分目录，与 server/plugin 对应）
│   │   ├── login/ dashboard/ profile/ error/（404 / 403）
│   │   ├── user/ role/ dept/ post/ menu/ data_rule/
│   │   └── config/ dict/ file/ crontab/ generator/ log/ online/ notice/ upgrade/
│   ├── router/index.ts           # 静态路由 + 动态路由（菜单驱动）+ 守卫 + NProgress
│   ├── stores/                   # Pinia：app / user / menu / notice / setting / worktab
│   ├── types/table.ts            # ArtTableColumn / PageResult
│   ├── utils/                    # auth / crypto / icon / theme / cron / progress
│   ├── App.vue                   # 仅 <router-view />
│   ├── main.ts                   # 应用引导（样式顺序 / 插件注册 / 首屏 loading 移除）
│   └── env.d.ts                  # 环境变量与 *.vue 类型声明
├── index.html                    # 挂载点 + 内联首屏 loading
├── vite.config.ts                # 别名 / 代理 / 分包
├── eslint.config.js              # ESLint 扁平配置
├── .prettierrc.json / .prettierignore
├── tsconfig.json / tsconfig.node.json
├── package.json
├── .env.development / .env.production   # VITE_API_BASE=/api
└── .gitignore
```

---

## 四、命名约定

### 4.1 后端

| 对象 | 约定 | 示例 |
|------|------|------|
| 插件 | 小写，无连字符 | `cccms`、`index`、`shop` |
| 控制器 | `{模块}Controller.php`，位于 `app/controller/` | `UserController.php` |
| 逻辑层 | `{模块}Logic.php`，位于 `app/logic/` | `UserLogic.php` |
| 模型 | 单数大驼峰，位于 `app/model/` | `User.php`、`CrontabLog.php` |
| 表名 | `sys_` 前缀（连接配置自动补全，模型里写 `user`） | `sys_user` / `User` |
| 权限节点 | `{插件}:{模块}:{动作}`，全小写，至少 3 段 | `cccms:user:save` |

### 4.2 前端

| 对象 | 约定 | 示例 |
|------|------|------|
| 页面 | `pages/{插件}/{模块}/index.vue` | `pages/cccms/user/index.vue` |
| 组件名 | **必须等于菜单 slug**（keep-alive 依赖） | `defineOptions({ name: 'cccms:user' })` |
| API 文件 | `api/{模块}.ts` | `api/user.ts`、`api/log.ts` |
| 组件 | `Art{名称}.vue`，位于 `components/core/` | `ArtTable.vue` |
| Store | `stores/{域}.ts`（组合式写法） | `stores/notice.ts` |
| 设计 token | `--art-{语义}` | `--art-primary`、`--art-card-bg` |
| 列宽记忆键 | `cccms_table_width:{路由或 storageKey}` | 与列显隐键 `cccms_table_hidden:` 并列 |
| 筛选方案键 | `cccms_table_filter_scheme:{路由或 storageKey}` | 单条方案见 `cccms_table_filter:` 前缀 |

### 4.3 「权限节点 / 控制器 / 页面」三者可互相推导

```
cccms:user:save
   │      │      └─ 动作 → UserController::save()
   │      └──────── 模块 → pages/cccms/user/index.vue
   └─────────────── 插件 → plugin/cccms/
```

这条推导链是 CCCMS 保持结构一致的关键：改一处，另外两处的位置可直接算出。

---

## 五、约定与边界

- **`plugin/cccms` 只放框架级基础功能**，一切业务功能必须新建业务插件，禁止往 `cccms` 插件里塞业务代码。
- 业务插件只要提供 `plugin/{插件}/db/menu.php`，就会被自动纳入菜单同步、权限扫描、数据权限检查与回收站类型扩展。
- 主应用 `server/app/` 保持干净；业务代码永远落在插件里。
- `server/runtime`、`server/public/storage` 属于运行时产物，不纳入版本控制。
