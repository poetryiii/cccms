# 前端组件参考

> 本文是 `components/core/`、`composables/`、`directives/`、`utils/`、`types/` 的 API 参考。用法约定见 [09-前端开发规范](09-前端开发规范.md)。

---

## 一、`ArtTable` — 列表页核心表格

文件：`src/components/core/ArtTable.vue`

集成了工具栏、搜索卡片、列头筛选、表格、分页、列显隐、回收站模式的通用表格。

### 1.1 Props

| Prop | 类型 | 默认 | 说明 |
|------|------|------|------|
| `columns` | `ArtTableColumn[]` | 必填 | 列配置，见 [§十二](#十二类型-arttablecolumn) |
| `data` | `Record<string, any>[]` | 必填 | 表格数据 |
| `loading` | `boolean` | `false` | 加载中 |
| `total` | `number` | `0` | 总条数 |
| `rowKey` | `string` | `'id'` | 行 key |
| `height` | `number \| string` | `'100%'` | 表格高度 |
| `pageSizes` | `number[]` | `[10,15,30,50,100]` | 每页条数候选 |
| `selection` | `boolean` | `false` | 是否显示多选列 |
| `pagination` | `boolean` | `true` | 是否显示分页（树表常设 `false`） |
| `tree` | `boolean` | `false` | 树形表格 |
| `storageKey` | `string` | — | 列显隐持久化的 key（默认取当前路由 path） |
| `recycle` | `boolean` | `false` | 回收站模式 |
| `virtual` | `boolean` | `false` | 大表格虚拟滚动（切换 `el-table-v2`） |

### 1.2 v-model

| 模型 | 默认 | 说明 |
|------|------|------|
| `page` | `1` | 当前页 |
| `limit` | `15` | 每页条数 |

### 1.3 Emits

| 事件 | 载荷 | 说明 |
|------|------|------|
| `refresh` | — | 点击刷新 |
| `page-change` | `page` | 页码变化 |
| `size-change` | `limit` | 每页条数变化 |
| `sort-change` | `{ prop, order }` | 排序变化（列需 `sortable: 'custom'`） |
| `selection-change` | `rows` | 多选变化 |
| `restore` | `row?` | 回收站：还原（不传为批量） |
| `force-delete` | `row?` | 回收站：彻底删除（不传为批量） |

### 1.4 Slots

| 插槽 | 说明 |
|------|------|
| `toolbar` | 工具栏左侧 |
| `toolbar-right` | 工具栏右侧（放新增按钮、`RecycleToggle`） |
| `pager-left` | 分页左侧 |
| 列 `col.slot` | 自定义列渲染，作用域 `{ row, index, value }` |

### 1.5 特性

- **列显隐按页面持久化**：`localStorage['cccms_table_hidden:v2:{storageId}']`（列默认全部显示）。
- **搜索卡片（表格上方）**：搜索项由各列的 `filter` 配置**自动生成**，页面零接线，改列即改搜索项。
  - 与表格卡片同级，单独一张卡放在表格**上方**；没有 `filter` 列时整卡不渲染。
  - 控件按 `filter.type` 生成：`text` → 输入框、`enum` → 多选下拉（折叠标签）、`date` → 日期范围选择器。
  - 与列头筛选**共用同一份 `query`**（同样经 `readFilter` / `writeFilters` 读写），因此两边天然联动：
    列头改了顶部的值跟着变，顶部查询后列头漏斗也高亮，条件同样被「筛选方案」持久化。
  - 草稿与列头弹层分开保存，**输入过程不发请求**；点「查询」把全部筛选字段一次性写回（只触发一次重新查询），
    「重置」清空全部筛选字段。回车即查询。
  - 搜索项超过 3 个（`SEARCH_COLLAPSE_LIMIT`）时默认折叠，只显示前 3 项，右侧给「展开 / 收起」入口
    （`table.searchExpand` / `table.searchCollapse`）。
- **列头筛选**：给列加 `filter` 配置后，该列表头出现筛选入口：
  - 列头用 `#header` 插槽渲染「标题 + 漏斗图标」，**漏斗图标固定在列头最右侧**（标题对齐跟随列的 `align`）；
    有生效条件时图标高亮为主题色。
  - `type: 'enum'` 弹层内是**复选下拉框**（`el-select multiple`，折叠标签 + 可清空），多值按逗号拼接；
  - `type: 'text'` 弹层内是输入框，做模糊查询，回车或点「筛选」提交；
  - `type: 'date'` 弹层内是**时间范围**选择器（`datetimerange`，值格式 `YYYY-MM-DD HH:mm:ss`，
    `defaultTime` 让手动选择默认落在 `00:00:00 ~ 23:59:59`），起止分别写入 `startKey` / `endKey`
    （默认 `start` / `end`）；内置 7 个常用区间快捷项：今天 / 昨天 / 最近 3 天 / 最近 7 天 /
    最近 30 天 / 本月 / 上月。同一页有多个时间条件时**务必各配一组独立的 `startKey` / `endKey`**，
    否则会互相覆盖（如在线用户的 `login_start` / `login_end`、公告的 `publish_start` / `publish_end`）；
  - 弹层底部按规范**重置在左、筛选在右**，均为按钮形式（筛选为主题色 `primary`）；
  - 不使用 el-table 原生 `filters`（其面板为纯文本按钮，且无法把入口固定在列头右侧），真正的过滤都在后端做；
  - 筛选值统一写 `query`，经 `readFilter` / `writeFilter` 读写，**自动被「筛选方案」持久化**；
    服务端分页场景 `writeFilter` 自动回第 1 页并重新查询。
  - **页面不要再手写搜索表单**：顶部搜索卡片已由列的 `filter` 配置自动生成，`ArtTable` 不提供
    `#search` 插槽与 `search` / `reset` emit。
- **列宽拖拽记忆**：拖动表头后按页面持久化到 `localStorage['cccms_table_width:{storageId}']`；
  已保存的宽度**优先于**页面声明的 `width`（用户意图优先）；「列设置 → 重置」会同时清掉列宽。
- 用可见列指纹作 `:key` 强制重挂载表格（解决列设置后布局不刷新）。
- **回收站模式**：工具栏左侧换成「回收站模式」标签 + 还原 / 彻底删除按钮（`v-auth` 校验 `cccms:recycle:restore` / `cccms:recycle:delete`）；`slot: 'action'` 列被统一替换为操作列。
- 响应式分页：窄屏自动切换为 `prev, pager, next`。
- 刻意**不开** `reserve-selection`（避免跨刷新残留脏勾选）。
- **批量操作**：开启 `selection` 的页面在工具栏放一个 `el-dropdown`（`批量操作（N）`，
  `:disabled="selected.length === 0"`），通过 `@selection-change` 维护 `selected`。
  每个 `el-dropdown-item` 各自挂 `v-auth`（如 `cccms:user:batch_status` / `batch_delete` / `batch_assign`）。
  后端返回 `{affected, skipped[]}`，**有跳过时提示降级为 `ElMessage.warning` 并说明原因**
  （越权 / 受保护 / 不存在），避免「点了没反应」的困惑。参考实现：
  `pages/cccms/user/index.vue`、`pages/cccms/post/index.vue`、`pages/cccms/dict/index.vue`。
  注意：`ArtTable` **必须显式传 `row-key="id"`**，否则勾选状态在数据刷新后错乱。
- **虚拟滚动（`virtual`）**：仅对**非树形、非回收站**的普通列表生效，命中 `tree` / `recycle` 时自动回退
  普通 `el-table`（行为完全不变），因此**业务页零改动**——只有确实存在「单页数千行以上」的列表才需要
  显式加 `virtual`。开启后内部改用 `el-auto-resizer` + `el-table-v2`：
  - 列的 `width` 为 `el-table-v2` 必填项，未声明 `width` / `minWidth` 的列按 `150px` 兜底；
    拖拽记忆的列宽（`cccms_table_width:*`）依然优先生效。
  - 列 `slot` 通过 `cellRenderer` 重建，作用域仍为 `{ row, index, value }`，页面插槽写法不变。
  - `selection` 由组件内部用 `ElCheckbox` 自绘表头全选 / 行勾选，并照常派发 `selection-change`，
    翻页或刷新后自动剔除已不在当前数据里的勾选（与普通表格不开 `reserve-selection` 的语义一致）。
  - `fixed: 'right'`（操作列）会映射为 `el-table-v2` 的右固定列，需保持为列数组的最后一项（现有页面均满足）。
  - 分页、列设置、列宽记忆、筛选方案等工具栏能力与普通模式共用，无需区分。

> 列宽持久化由组件内部处理（监听 el-table 的 `header-dragend`），页面无需任何额外代码。

---

## 二、`ArtSplitView` — 左右分栏

文件：`src/components/core/ArtSplitView.vue`

| Prop | 类型 | 默认 |
|------|------|------|
| `asideWidth` | `string` | `'248px'` |

| Slot | 说明 |
|------|------|
| `aside` | 左栏 |
| 默认 | 右栏主内容 |

- `≤ 900px` 自动上下堆叠。
- 宽度由 CSS 变量 `--art-split-aside` 控制。

---

## 三、`ArtTreePanel` — 左侧树面板

文件：`src/components/core/ArtTreePanel.vue`

| Prop | 类型 | 默认 |
|------|------|------|
| `data` | `any[]` | 必填 |
| `title` | `string` | `''` |
| `nodeKey` | `string` | `'id'` |
| `labelKey` | `string` | `'name'` |
| `filterable` | `boolean` | `true` |
| `expandAll` | `boolean` | `true` |
| `currentKey` | `string \| number \| null` | `null` |

| Emit | 说明 |
|------|------|
| `node-click(data)` | 点击节点 |

| Slot | 说明 |
|------|------|
| `header` | 标题右侧操作区 |
| `node` | 自定义节点渲染（`v-bind="scope"`） |

数据加载与增删改由页面负责，组件只负责展示与高亮。

---

## 四、`ArtIcon` — 图标

文件：`src/components/core/ArtIcon.vue`

| Prop | 类型 | 说明 |
|------|------|------|
| `name` | `string` | 图标名（如 `icon-user`），经 `utils/icon.ts` 的 `resolveIcon()` 解析 |

```vue
<el-icon><ArtIcon name="icon-user" /></el-icon>
```

- 只渲染图标本身；外层需自行用 `<el-icon>` 包裹。
- 找不到时回退为 `Menu` 图标；兼容带 / 不带 `icon-` 前缀。

---

## 五、`ArtNodePicker` — 节点选择器

文件：`src/components/core/ArtNodePicker.vue`

用于数据权限规则表单的「绑定对象」选择，支持**树模式**与**远程搜索模式**。

| Prop | 类型 | 默认 | 说明 |
|------|------|------|------|
| `modelValue` | `number \| number[] \| null` | — | 单选 `number`，多选 `number[]`（`0` 视为未选） |
| `data` | `any[]` | `[]` | 树模式数据 |
| `search` | `(kw: string) => Promise<any[]>` | — | 远程模式搜索函数 |
| `selectedRow` | `any` | — | 远程模式回显 |
| `labelFn` | `(row) => string` | — | 标签格式化 |
| `nodeKey` | `string` | `'id'` | |
| `labelField` | `string` | `'name'` | |
| `multiple` | `boolean` | `false` | |
| `placeholder` | `string` | `'输入关键词搜索'` | |

| Emit | 说明 |
|------|------|
| `update:modelValue` | |

- **树模式**（岗位 / 角色 / 部门）：`check-strictly` 复选树 + 展开 / 全选 / 清空按钮。
- **远程模式**（用户）：搜索框 + 扁平勾选列表，已选中钉最前，输入防抖 250ms。

---

## 六、`ArtSettingsDrawer` — 主题设置抽屉

文件：`src/components/core/ArtSettingsDrawer.vue`

| v-model | 说明 |
|---------|------|
| `visible` | 显隐（必填） |

内容：主题模式（亮 / 暗 / 跟随系统）、品牌主色（8 个预设 + 自定义取色）、圆角（0–20px）、内容区宽度（0–2400px，步进 120），以及「跟随系统默认」重置。

---

## 七、`CronEditor` — Cron 可视化编辑器

文件：`src/components/core/CronEditor.vue`

| v-model | 说明 |
|---------|------|
| `expression` | 六段表达式字符串（必填） |

- 宝塔式引导：选周期（每秒 / 每 N 秒 / 每 N 分钟 / 每 N 小时 / 每天 / 每周 / 每月 / 自定义）→ 填时间。
- 底部回显表达式 + 中文说明 + **最近 3 次运行时间**。
- 依赖 `utils/cron.ts`（`buildFromCycle` / `detectCycle` 往返一致）。
- 日与周同时限定按 **AND**（与后端一致）。

---

## 八、`RecycleToggle` — 回收站开关

文件：`src/components/core/RecycleToggle.vue`

| Prop | 类型 | 默认 |
|------|------|------|
| `active` | `boolean` | 必填 |
| `label` | `string` | `''` |

| Emit | 说明 |
|------|------|
| `toggle` | 切换 |

- 内部带 `v-auth="['cccms:recycle:restore', 'cccms:recycle:delete']"`（任一权限可见）。
- 激活时高亮。通常放在 `ArtTable` 的 `#toolbar-right`。
- `label` 是模块名词，作为 tooltip 模板的参数传入：整句由语言包拼装（`table.enterRecycle` /
  `table.exitRecycle`），**不要**在调用方拼串。用法：`:label="t('user.recycleLabel')"`。

---

## 九、`useTable` — 列表页通用逻辑

文件：`src/composables/useTable.ts`

```ts
interface UseTableOptions<T, Q extends object> {
  api: (params: Record<string, unknown>) => Promise<PageResult<unknown>>
  initialQuery?: Q
  pageSize?: number       // 默认取后台配置 ui.page_size
  immediate?: boolean     // 默认 true，setup 阶段立即加载
  transform?: (list: T[]) => T[]
}

function useTable<T, Q extends object>(options: UseTableOptions<T, Q>)
```

返回值：

| 返回 | 说明 |
|------|------|
| `list` | 列表数据 |
| `loading` | 加载状态（**至少可见 300ms**，避免一闪而过） |
| `total` / `page` / `limit` | 分页 |
| `query` | 查询条件对象（响应式） |
| `selection` | 当前勾选行 |
| `load()` | 加载（参数为 `{ ...query, page, limit }`） |
| `search()` | 重置到第 1 页再加载 |
| `reset()` | 恢复 `initialQuery` 并回第 1 页 |
| `onPageChange` / `onLimitChange` | 分页事件处理 |
| `onSelectionChange` | 多选变化处理 |

**筛选方案持久化（页面零配置）**：

- 条件（含 `limit`，不含 `page`）一变就自动写入 `localStorage['cccms_table_filter:{storageId}']`；下次进入同页面**自动回填**（不自动提交，避免误触发请求）。
- 命名方案整体存 `localStorage['cccms_table_filter_scheme:{storageId}']`；`storageId` 默认取当前路由 path，可经 `storageKey` 覆盖。
- 回填只保留 `initialQuery` 声明过的字段（白名单），避免旧版本遗留字段被重新发给接口；页面未声明 `initialQuery` 时退化为原样回填。
- 这些能力**不经 `useTable` 返回值暴露**，而是通过 `provide(TABLE_FILTER_KEY, …)` 供 `ArtTable` 工具栏的「筛选方案」入口消费：

| 上下文成员 | 说明 |
|------|------|
| `key` | 当前页面的 storageId |
| `hasSaved()` | 是否存在上次保存的条件（决定是否显示「清空记忆」） |
| `clearSaved()` | 清空自动记忆的条件 |
| `listSchemes()` | 已保存方案名列表（`string[]`） |
| `saveScheme(name)` | 以 `name` 保存当前条件（同名覆盖） |
| `applyScheme(name)` | 应用方案（回填并回到第 1 页重新加载） |
| `removeScheme(name)` | 删除方案 |
| `readFilter(prop)` | 读取该字段当前值（列头筛选与顶部搜索卡片共用） |
| `writeFilter(prop, value)` | 写入该字段并重新查询（树表场景由 computed 重算） |
| `writeFilters(fields)` | 批量写入多个字段，只触发一次重新查询（顶部搜索卡片的「查询 / 重置」用它） |

**树表等纯前端过滤场景用 `useTableFilter`**：

`menu` / `dept` 这类树表**一次性拉全量、靠 computed 过滤**，没有分页与请求，用不上 `useTable`。
`useTable` 把筛选方案内核抽成 `useTableFilter()` 供它们复用（同一份 `localStorage` 键与同一个
`TABLE_FILTER_KEY` 入口，所以 `ArtTable` 工具栏的「筛选方案」照常出现）：

```ts
// 只返回条件对象与重置函数，不涉及分页 / 请求
const { query, reset } = useTableFilter<Query>({ initialQuery: { name: '', leader: '' } })
```

- 条件变化后由页面自己的 `computed` 响应（无需重新请求）；应用命名方案同理。
- 条件同样会自动记忆、自动回填，字段白名单规则与 `useTable` 一致。
- 树形过滤建议：**命中节点保留整棵子树**；未命中但子孙命中的节点保留自身、`children` 换成过滤结果。

**列显隐**：

- 列**默认全部显示**，不存在「默认隐藏某列」的配置（`ArtTableColumn` 无 `defaultHidden` 字段）。
- 用户仍可在工具栏「列设置」popover 里**手动**隐藏 / 恢复列，并点「重置」回到全显。
- 隐藏结果存 `localStorage['cccms_table_hidden:v2:{storageId}']`（前缀带版本号，改动默认显隐策略时升版本即可让旧缓存一次性失效）。

---

## 十、`useRecycle` — 回收站

文件：`src/composables/useRecycle.ts`

```ts
interface RecycleOptions {
  reload: () => void       // 切换模式 / 操作完成后重新加载
  ids?: () => number[]     // 批量操作用的当前勾选 id
  clear?: () => void       // 清空勾选
}

function useRecycle(type: string, options: RecycleOptions)
// → { recycle, toggle, onRestore, onForceDelete }
```

- `type` 是后端 `RecycleLogic` 注册表里的类型（`user` / `role` / `dept` / `post` / `dict_type` / `dict_data` / `category` / `crontab` / `data_rule` / `file` / `menu`）。
- `onRestore(row?)` / `onForceDelete(row?)`：不传 `row` 用 `ids?.()`（批量），传 `row` 用其 id；均二次确认后调接口，再 `clear` + `reload`。

> ⚠️ **必须在 `useTable` 之前调用**（列表闭包在 setup 阶段就会执行一次）。三处接线见 [07-功能模块 §十六](07-功能模块.md#十六回收站recycle)。

---

## 十一、指令与工具

### 11.1 `v-auth`

文件：`src/directives/index.ts`（`setupDirectives(app)` 注册）

```vue
<el-button v-auth="'cccms:user:save'">新增</el-button>
<el-button v-auth="['cccms:recycle:restore', 'cccms:recycle:delete']">回收站</el-button>
```

- 不通过时 `display: none`（不移除 DOM）。
- 判断依据 `userStore.hasAuth()`：超管直通，普通用户命中任一即通过。

### 11.2 `downloadFile`（`api/request.ts`）

| 方法 | 说明 |
|------|------|
| `downloadFile(url, params?, fallbackName?)` | 拉取后端文件流并触发浏览器下载；复用同一 axios 实例（鉴权头 / 401 处理 / 进度条），文件名取 `Content-Disposition`（支持 RFC 5987 的 `filename*`） |

```ts
import { downloadFile } from '@/api/request'
await downloadFile('/log/export', { username: 'admin' }, '操作日志.csv')
```

> 约定：导出接口返回**文件流**而非统一信封。响应拦截器对 `responseType === 'blob'` 的请求
> 保留完整响应，其余请求仍按 `code === 0` 解包 `data`。

### 11.3 `utils/auth.ts`

| 方法 | 说明 |
|------|------|
| `getToken()` / `setToken()` / `clearToken()` | localStorage，key `cccms_token` |

### 11.4 `utils/crypto.ts`

| 方法 | 说明 |
|------|------|
| `decryptField(encoded, keyBase64)` | AES-256-GCM 解密字段级 `encrypt` 值；后端密文为 `base64(iv(12) || tag(16) || cipher)`（WebCrypto 需拼接 `cipher || tag`） |

配合 `userStore.cryptoKey`（来自 `/auth/me` 的 `crypto_key`）。

> ⚠️ **只对持有 `cccms:data_rule*` 权限节点的管理员与超管下发**（`AuthLogic::mayDecryptFields()`）：
> 它是对称密钥的公开形式，拿到即可解密全部密文，因此**不构成访问控制**。
> 敏感字段请用 `mask` + 数据权限，理由与取舍见 [06-数据权限 §11.1](06-数据权限.md#111-安全边界encrypt-是存储加密不是访问控制)。

### 11.5 `utils/icon.ts`

| 导出 | 说明 |
|------|------|
| `ICON_MAP` | `icon-xxx` → Element Plus 图标组件（只登记用得到的，保证 tree-shaking） |
| `MENU_ICON_OPTIONS` | 菜单图标选择器候选 |
| `resolveIcon(name)` | 解析图标，找不到回退 `Menu` |

### 11.6 `utils/theme.ts`

| 导出 | 说明 |
|------|------|
| `ThemeMode` / `ThemeConfig` / `PRESET_COLORS` | 类型与预设 |
| `mixColor(color, target, weight)` | 颜色混合 |
| `resolveMode(mode)` | `auto` 取系统偏好 |
| `applyPrimary(color)` | 主色（同时重算 `--el-color-primary-*`） |
| `applyRadius(px)` / `applyContainerWidth(px)` | |
| `applyTheme(config)` | 应用整套主题（带 `theme-transition-off` 关过渡） |

### 11.7 `utils/cron.ts`

| 导出 | 说明 |
|------|------|
| `nextRunTimes(expr, count, from?)` | 未来 N 次运行时间 |
| `formatCronTime(ts)` | 格式化 |
| `CRON_RANGES` | 各段取值范围 |
| `buildFromCycle(state)` / `detectCycle(value)` | 周期对象 ↔ 表达式（往返一致） |
| `CronCycle` / `CronCycleState` | 类型 |

### 11.8 `utils/progress.ts`

NProgress 统一入口。

| 导出 | 说明 |
|------|------|
| `ROUTE_PROGRESS` / `UPLOAD_PROGRESS` | 预置令牌 |
| `progressStart(token)` / `progressDone(token)` | 首个任务才真正 start，最后任务结束才 done；同令牌 start 幂等，未知令牌 done 忽略 |

---

## 十二、类型 `ArtTableColumn`

文件：`src/types/table.ts`

```ts
export interface ArtTableColumn {
  prop: string
  label: string
  width?: number | string
  minWidth?: number | string
  fixed?: boolean | 'left' | 'right'
  align?: 'left' | 'center' | 'right'
  sortable?: boolean | 'custom'     // 'custom' 需自行监听 sort-change
  slot?: string                     // 具名插槽名 <template #xxx="{ row, value }">
  lockVisible?: boolean             // 锁定可见（不可隐藏）
  showOverflowTooltip?: boolean
  filter?: ArtTableColumnFilter     // 筛选配置：列表头出现筛选入口，同时自动生成顶部搜索项
}

export interface ArtTableColumnFilter {
  type: 'enum' | 'text' | 'date'    // enum：复选下拉；text：模糊查询输入框；date：时间范围选择器（datetimerange）
  options?: ArtTableFilterOption[]  // type='enum' 时必填
  queryKey?: string                 // 写入 query 的字段名，默认取列的 prop
  placeholder?: string              // type='text' 时的输入框占位文案
  startKey?: string                 // type='date' 时的起始字段名，默认 start
  endKey?: string                   // type='date' 时的结束字段名，默认 end
}
```

筛选的读写经 `TABLE_FILTER_KEY` 上下文的 `readFilter(prop)` / `writeFilter(prop, value)` /
`writeFilters(fields)` 落到页面 `query` 上（服务端分页场景会自动回第 1 页并重新请求，树表场景由 computed 重算）。
列头筛选与顶部搜索卡片**同源**：搜索项就是带 `filter` 的列，两边读写同一份 `query`。

另重导出 `PageResult<T>`（`{ total, list }`）。

---

## 十三、样式约定（`assets/styles/`）

| 文件 | 职责 |
|------|------|
| `index.css` | 入口：`@import 'tailwindcss'` → `tokens.css`；把 token 映射为 Tailwind 工具类；基础 reset |
| `tokens.css` | **设计 token 唯一来源**：亮色 `:root`，暗色 `html.dark` |
| `element.css` | Element Plus 主题覆盖（全用 EP CSS 变量，暗色随 `html.dark`） |
| `app.css` | 业务全局：页面高度链 `.art-fill` / `.art-scroll` / `.art-page`、通用卡片、路由过渡、进度条跟随主色 |

常用工具类：

| 类 | 用途 |
|----|------|
| `.art-fill` | 列表页根节点，撑满高度，表格内部滚动 |
| `.art-scroll` | 普通页根节点，整体滚动 |
| `.art-page` | 居中容器（配合 `--art-container-width`） |
| `.art-card` / `.art-card-body` | 通用卡片 |
