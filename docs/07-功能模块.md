# 功能模块

> 本文逐个模块说明：功能是什么、涉及哪些表、有哪些接口、前端在哪个页面、对应哪些权限节点、有哪些坑。接口路径均为前端 `baseURL`（`/api`）之后的真实后端路径。

---

## 〇、阅读约定

> 本文按模块讲「功能 / 接口 / 页面 / 权限节点」；**表结构与字段语义统一见 [13-数据模型](13-数据模型.md)**，本文不再重复字段定义。

- **权限节点**列只列 `#[Permission]` 的 slug；未列出的方法为 `#[NoAuth]` / `#[NoLogin]`。
- 所有写操作接口都带 `#[Restrict(methods: ['POST'])]`。
- 所有接口返回统一信封 `{code, message, data}`，成功 `code = 0`。
- 前端页面统一位于 `frontend/src/pages/cccms/{模块}/index.vue`。

---

## 一、工作台（Dashboard）

| 项 | 内容 |
|----|------|
| 功能 | 欢迎卡 + 统计卡（用户 / 角色 / 部门 / 岗位 / 菜单 / 今日日志）+ 7 天日志趋势折线 + 附件类型分布饼图 |
| 表 | 只读聚合，不新增表 |
| 接口 | `GET /dashboard/stats`（`#[NoAuth]`，登录即可） |
| 前端 | `pages/cccms/dashboard/index.vue`，组件名 `dashboard`，静态路由 `/dashboard` |
| 权限节点 | 无（登录即可） |

注意事项：

- **统计口径与列表页一致**：`user` / `dept` / `file` / `log` 的计数按当前用户数据范围聚合，避免「列表只能看到 3 条、工作台显示 1000 条」的绕行泄漏。
- 用户 / 角色 / 部门 / 岗位只统计 `status = 1`；菜单只统计 `type = 2`。
- 日志趋势逐天补 0；附件类型按扩展名分组取前 6。
- ECharts 按需引入（`echarts/core`），主题切换时重渲染。

---

## 二、认证（Auth）

| 项 | 内容 |
|----|------|
| 功能 | 健康检查、登录、图形验证码、当前用户信息、登出 |
| 表 | `sys_user`、`sys_role`、`sys_user_role`、`sys_role_node`、`sys_tenant`（校验租户可用性） |
| 前端 | `pages/cccms/login/index.vue` |

| 方法 | 接口 | 注解 | 说明 |
|------|------|------|------|
| `ping` | `GET /ping` | `#[NoLogin]` | 健康检查，返回 `pong` + 时间 |
| `login` | `POST /auth/login` | `#[NoLogin]` | 账号密码（+ 可选验证码）登录，返回 token + 用户信息 |
| `captcha` | `GET /auth/captcha` | `#[NoLogin]` | 图形验证码；未开启时返回 `null` |
| `me` | `GET /auth/me` | `#[NoAuth]` | 当前用户信息（含 `crypto_key`） |
| `logout` | `POST /auth/logout` | `#[NoAuth(title:'注销')]` | 注销：服务端把当前 `jti` 写入黑名单并移除在线会话 |
| `tenants` | `GET /auth/tenants` | `#[NoAuth]` | 可切换的租户下拉：超管返回全部租户，其他账号只返回当前租户 |
| `switchTenant` | `POST /auth/switchTenant` | `#[NoAuth(title:'切换租户')]` | 超管显式切换租户：重签带 `tid` 声明的令牌 + 迁移在线会话 `jti` |

登录校验顺序（`AuthLogic::login`）：

1. `LoginThrottle::assertNotLocked()`（锁定 → 429）。
2. `security.login_captcha` 开启才校验验证码。
3. `User::withoutAllScopes()` 查账号（**用户名全局唯一 + 回收站账号不可登录**）+ `password_verify`。
4. 状态 `status !== 1` → 403。
5. `AuthService::buildContext()` 失败 → 「账号角色异常」。
6. 维护模式下仅超管可登录。
7. 成功后清失败计数、更新 `login_time` / `login_ip`。

注意事项：

- 登录成功后返回的 `user.permissions` 是前端 `v-auth` 的数据来源。
- 登出**已做服务端失效**：把当前 `jti` 写入 Redis 黑名单（TTL = 剩余有效期）+ 移除在线会话，
  `CheckLogin` 每次校验；令牌损坏 / 已过期时按「登出成功」处理，保证接口幂等。
- 登录成功 / 失败都会写入操作日志（`sys_log`，`path='/auth/login'`），见 [十三、操作日志](#十三操作日志log)。
- **租户可用性逐请求校验**：`AuthService::buildContext()` 每次都会检查 `tid` 对应租户是否存在 / 未删除 /
  已启用 / 未过期 —— 租户被停用或到期后，该租户下所有账号**下一次请求即 401**，与「禁用用户立即生效」同一口径。
  详见 [二十一、租户管理](#二十一租户管理tenant)。

---

## 三、个人中心（Profile）

| 项 | 内容 |
|----|------|
| 功能 | 查看本人资料、修改本人基本资料、修改本人密码、查看与注销本人登录设备 |
| 表 | `sys_user`、`sys_role`、`sys_user_dept`、`sys_dept` |
| 接口 | `GET /profile`、`POST /profile/update`、`POST /profile/password`、`GET /profile/sessions`、`POST /profile/sessions/revoke`（全部 `#[NoAuth]`） |
| 前端 | `pages/cccms/profile/index.vue`，**静态路由** `/profile`（不进左侧菜单，入口在右上角用户下拉） |
| 权限节点 | 无 |

注意事项：

- 目标**始终是 `$user->id`**，不接受外部传入 id（防越权）。
- 可改字段白名单：`nickname` / `avatar` / `email` / `phone`（不含 username / status / 角色 / 部门 / 岗位）。
- 改密需校验原密码 + `PasswordPolicy` + 新旧不能相同。
- 走 `User::withoutGlobalScope()` 逃生口，否则「本部门档 + 未分配部门」的用户会被数据权限 fail-closed 挡住读自己资料。
- **登录设备**：`GET /profile/sessions` 通过 `OnlineSession::listOfUser()` 仅返回 `user_id = 当前用户`
  的会话（含 `current` 标记）；`POST /profile/sessions/revoke` 用 `OnlineSession::ownerOf()` 校验归属，
  失败统一返回 404「会话不存在或已失效」（**不区分不存在 / 不是我的**，避免探测他人会话）；
  禁止注销当前会话（422，改用「退出登录」）；注销时 `TokenBlacklist::revoke()` + `OnlineSession::remove()`
  **两处同时生效**。设备 / 系统 / 浏览器由 UA 派生，口径与 `GET /online` 一致。

---

## 四、用户管理（User）

| 项 | 内容 |
|----|------|
| 功能 | 用户增删改查、分配角色 / 部门（多选）/ 岗位（多选）、重置密码 |
| 表 | `sys_user`、`sys_user_role`、`sys_user_dept`、`sys_user_post` |
| 前端 | `pages/cccms/user/index.vue`，组件名 `cccms:user` |

| 接口 | 权限节点 | 说明 |
|------|----------|------|
| `GET /user` | `cccms:user:index` | 分页列表（安全字段，不含 password） |
| `GET /user/read` | `cccms:user:read` | 详情（含 `role_ids` / `dept_ids` / `post_ids`） |
| `POST /user/save` | `cccms:user:save` | 新增（密码必填 + 密码策略） |
| `POST /user/update` | `cccms:user:update` | 更新（密码留空则不改） |
| `POST /user/delete` | `cccms:user:delete` | 软删除（不能删自己、不能删超管） |
| `POST /user/resetPassword` | `cccms:user:reset_password` | 重置密码 |
| `GET /user/export` | `cccms:user:export` | 导出 CSV（按当前筛选与数据范围，直接返回文件流） |
| `GET /user/template` | `cccms:user:template` | 下载导入模板（CSV） |
| `POST /user/import` | `cccms:user:import` | 导入 CSV（新增 / 更新，行级失败汇总返回） |
| `POST /user/batchStatus` | `cccms:user:batch_status` | 批量启用 / 禁用（`{ids, status}`） |
| `POST /user/batchDelete` | `cccms:user:batch_delete` | 批量软删除 |
| `POST /user/batchAssign` | `cccms:user:batch_assign` | 批量分配角色 / 部门 / 岗位（整体替换语义） |

注意事项：

- 数据权限全由模型层承担（行级范围 + 出参字段规则 + 入参剔除）。
- 唯一性 / 存在性校验显式 `withoutGlobalScope()`（含 `withTrashed`，**回收站账号仍占用用户名**）。
- 删除为软删，关联表（user_role / user_dept / user_post）保留。
- **批量操作**统一语义：**只作用于可见行 + 跳过并回报 + 不整批失败**。先经
  `User::newScopedQuery()->whereIn('id', $ids)` 取可见交集，再剔除「超管账号」与「操作者自己」，
  返回值固定为 `{affected, skipped[]}`；`skipped` = 越权 / 受保护 / 不存在的 id。
  批量禁用后对方**下一次请求即 401**（鉴权每请求查库）。
  `batchAssign` 为**整体替换**：传了哪个键就重设哪个关联，三个键都没传抛 422。
- **导出**：最多 5000 行；列含角色名（批量查询，避免 N+1）。
- **导入**：`username` 必填；已存在的用户走 `update`（受数据范围约束），新用户**必须提供 `password`**
  （不内置弱口令默认值）；单行失败不影响其余行，失败原因按行号汇总返回。
  列约定：`username, nickname, email, phone, status, password, roles, depts, posts`；
  后三列多值用 `|` 分隔，`roles` / `posts` 支持**角色 / 岗位编码或名称**，`depts` 按**部门名称**匹配
  （部门名可重名，匹配不到按行失败返回）；留空表示不动该用户的分配关系。
  导出与导入均使用 `support/Csv.php`，带 UTF-8 BOM 与**公式注入防护**。

---

## 五、角色管理（Role）

| 项 | 内容 |
|----|------|
| 功能 | 角色增删改查、角色树、节点授权（含继承展示）、数据范围设置 |
| 表 | `sys_role`、`sys_role_node`、`sys_user_role` |
| 前端 | `pages/cccms/role/index.vue`，组件名 `cccms:role`（树形页：`ArtSplitView` + `ArtTreePanel`） |

| 接口 | 权限节点 | 说明 |
|------|----------|------|
| `GET /role` | `cccms:role:index` | 列表（可按 `node_id` 取子树） |
| `GET /role/tree` | `cccms:role:tree` | 角色树 |
| `GET /role/read` | `cccms:role:read` | 详情 + 节点（`own` / `inherited` 分开返回） |
| `POST /role/save` | `cccms:role:save` | 新增 |
| `POST /role/update` | `cccms:role:update` | 更新 |
| `POST /role/delete` | `cccms:role:delete` | 软删 |
| `POST /role/copy` | `cccms:role:copy` | 复制角色（含档位与自身节点授权，不复制子角色） |

注意事项：

- 超管角色（`code = super_admin`）**禁止编辑 / 删除**，`code` 不可改。
- 有子角色的角色不可删除。
- 保存节点授权时剔除 `nodes` / `own` / `inherited` 键（避免 `fields_strict` 报错）。
- 授权界面上，从父角色继承来的节点**只读**。
- 删除角色时 `role_node` / `user_role` 保留（彻底删除时才清）。
- **复制角色**：只复制角色本体与**自身** `role_node` 授权（不含继承来的祖先节点，避免源角色调整后副本失效），
  不复制子角色。**必须显式复制 `data_scope`** —— 档位刻意不继承（见 [05-鉴权与权限](05-鉴权与权限.md)），
  漏复制会让副本静默落到默认「仅本人」。`name` 缺省「源名 + 副本」，`code` 自动派生 `_copy` / `_copy2`…；
  父角色做存在性 + 深度 ≤5 校验（同时兜住历史脏数据中的环）；传 `status=0` 即「另存为模板」，
  **不传 `status` 时沿用源角色的状态**（不覆盖）。

---

## 六、部门管理（Dept）

| 项 | 内容 |
|----|------|
| 功能 | 无限级部门树的增删改查；用户可多部门 |
| 表 | `sys_dept`、`sys_user_dept`、`sys_dept_role` |
| 前端 | `pages/cccms/dept/index.vue`，组件名 `cccms:dept`（树形页） |

| 接口 | 权限节点 |
|------|----------|
| `GET /dept/tree` | `cccms:dept:tree` |
| `POST /dept/save` | `cccms:dept:save` |
| `POST /dept/update` | `cccms:dept:update` |
| `POST /dept/delete` | `cccms:dept:delete` |

注意事项：

- 部门模型**参与**数据权限：「本部门及以下」档下部门管理页只见自己子树。
- **作用域过滤后树的根节点处理**：可见节点的父节点可能不在集合内（例如只看自己所属的二级部门），这类节点会被**提升为顶层**，否则整棵树会因为找不到 `parent_id = 0` 的根而变空。
- 删除前检查子部门与成员（**看全量**，不受数据范围影响），软删。
- 「绑定部门」候选项用 `DeptLogic::treeAll()`（全量）：规则是配置动作，不能因为看不见某个部门就绑不了。

---

## 七、岗位管理（Post）

| 项 | 内容 |
|----|------|
| 功能 | 岗位增删改查 |
| 表 | `sys_post`、`sys_user_post` |
| 前端 | `pages/cccms/post/index.vue`，组件名 `cccms:post` |

| 接口 | 权限节点 |
|------|----------|
| `GET /post` | `cccms:post:index` |
| `POST /post/save` | `cccms:post:save` |
| `POST /post/update` | `cccms:post:update` |
| `POST /post/delete` | `cccms:post:delete` |
| `POST /post/batchStatus` | `cccms:post:batch_status` |
| `POST /post/batchDelete` | `cccms:post:batch_delete` |

注意事项：

- `Post` 声明**不参与**数据权限；删除前检查岗位下是否有用户，软删。
- **批量操作**语义同用户模块（只作用于可见行 + 跳过并回报）。批量删除时**下挂用户的岗位计入 `skipped`**
  而非抛异常，避免一个被占用的岗位让整批删不掉。

---

## 八、菜单管理（Menu）

| 项 | 内容 |
|----|------|
| 功能 | 菜单 / 目录 / 按钮节点树的管理；图标选择 |
| 表 | `sys_menu`、`sys_role_node` |
| 前端 | `pages/cccms/menu/index.vue`，组件名 `cccms:menu`（树表，`pagination=false`） |

| 接口 | 权限节点 | 说明 |
|------|----------|------|
| `GET /menu/tree` | `cccms:menu:tree` | 全量菜单树（`trashed=1` 平铺已删） |
| `GET /menu/userTree` | `#[NoAuth]` | 当前用户可见菜单树（前端动态路由数据源） |
| `POST /menu/save` | `cccms:menu:save` | 新增 |
| `POST /menu/update` | `cccms:menu:update` | 更新 |
| `POST /menu/delete` | `cccms:menu:delete` | 软删（**保留 role_node 授权**） |

注意事项：

- `type`：`1` 目录 / `2` 菜单 / `3` 按钮；按钮节点由 `perm-scan` 生成，**不手工维护**。
- 删除菜单时保留 `role_node` 授权：菜单恢复后授权自动重新生效（鉴权侧会自动排除回收站菜单）。
- 目录 / 菜单的声明源是 `db/menu.php`；`menu-sync` 以声明文件为权威，会覆盖 `parent_id` / `title` / `path` / `component` / `icon` / `sort`。
- 标题 i18n：库里的 `title` 是中文兜底值，`MenuLogic::localizeTitles()` 会按 `menu.{slug}` 命中才覆盖为当前语言；**管理员自建菜单没有对应 slug，原样返回**（用户录入数据不翻译）。

---

## 九、数据权限（DataRule / DataScopeTable）

| 项 | 内容 |
|----|------|
| 功能 | 行级 / 字段级数据权限规则的增删改查；受控表管理 |
| 表 | `sys_data_rule`、`sys_data_scope_table` |
| 前端 | `pages/cccms/data_rule/index.vue`，组件名 `cccms:data_rule` |

**规则接口**

| 接口 | 权限节点 |
|------|----------|
| `GET /data_rule` | `cccms:data_rule:index` |
| `GET /data_rule/options` | `cccms:data_rule:options` |
| `GET /data_rule/users` | `cccms:data_rule:users` |
| `POST /data_rule/save` | `cccms:data_rule:save` |
| `POST /data_rule/update` | `cccms:data_rule:update` |
| `POST /data_rule/delete` | `cccms:data_rule:delete` |
| `GET /data_rule/export` | `cccms:data_rule:export` |
| `GET /data_rule/template` | `cccms:data_rule:template` |
| `POST /data_rule/import` | `cccms:data_rule:import` |

**受控表接口**

| 接口 | 权限节点 |
|------|----------|
| `GET /data_rule/table` | `cccms:data_rule:table_index` |
| `POST /data_rule/table/save` | `cccms:data_rule:table_save` |
| `POST /data_rule/table/update` | `cccms:data_rule:table_update` |
| `POST /data_rule/table/delete` | `cccms:data_rule:table_delete` |

注意事项：

- 保存时校验：`table_name` 必须已在受控表内（`DataScope::canGuard`）、`field` 必须是合法标识符且**真实存在于目标表**、`operator` / `value_type` / `bind_mode` 走白名单。
- 非 `row` 动作会清空 `operator` / `value` / `value_type`。
- 列表 `decorate()` 会翻译绑定名 / 部门名 / 表名，并附上 `RuleConflict` 冲突体检结果（前端「检测」列）。
- 删除受控表登记**只删登记行、不删规则**，返回受影响规则数（提示「规则暂停生效」）。
- 规则表单：四个绑定维度用**选项卡**（用户 / 岗位 / 角色 / 部门），标题带已选数量；「添加到已选」按钮把草稿写入规则；已选列表在所有选项卡之外，可逐个 `×` 移除。
- **导入 / 导出**：模板下载 + 按当前筛选导出 + CSV 导入（以 `name` 为业务键，同名更新、否则新增；
  行级失败汇总）。列约定与校验细节见 [06-数据权限](06-数据权限.md#十二规则导入--导出)。
- 机制细节见 [06-数据权限](06-数据权限.md)。

---

## 十、数据字典与通用分类（Dict / Category）

| 项 | 内容 |
|----|------|
| 功能 | 字典类型 + 字典数据两级结构；两者都可归类到「通用分类」 |
| 表 | `sys_dict_type`、`sys_dict_data`、`sys_category`（`module = 'dict'`） |
| 前端 | `pages/cccms/dict/index.vue`，组件名 `cccms:dict`（树形页 + 抽屉） |

| 接口 | 权限节点 |
|------|----------|
| `GET /dict` | `cccms:dict:index` |
| `POST /dict/save` | `cccms:dict:save` |
| `POST /dict/update` | `cccms:dict:update` |
| `POST /dict/delete` | `cccms:dict:delete` |
| `GET /dict/data` | `cccms:dict:data` |
| `POST /dict/saveData` | `cccms:dict:save_data` |
| `POST /dict/updateData` | `cccms:dict:update_data` |
| `POST /dict/deleteData` | `cccms:dict:delete_data` |
| `GET /dict/category` | `cccms:dict:category` |
| `POST /dict/category/save` | `cccms:dict:category_save` |
| `POST /dict/category/update` | `cccms:dict:category_update` |
| `POST /dict/category/delete` | `cccms:dict:category_delete` |
| `POST /dict/batchStatus` | `cccms:dict:batch_status` |
| `POST /dict/batchDelete` | `cccms:dict:batch_delete` |
| `POST /dict/batchStatusData` | `cccms:dict:batch_status_data` |
| `POST /dict/batchDeleteData` | `cccms:dict:batch_delete_data` |

注意事项：

- 删除字典类型会**连带其字典数据一起软删**。
- 唯一键不做软删特例：回收站里的 type 仍占用，复用需先彻底删除。
- 通用分类 `CategoryLogic` 模块白名单 `['dict', 'file']`，可层级；`scopeIds()` 约定：空 / `0` = 不筛选，`-1` = 未分类，`> 0` = 该分类及所有下级。
- 分类更新禁止把自己 / 自己的下级设为上级（防环）。
- **缓存**：`GET /dict/data` 走 `support/DictCache.php`（`cccms:dict:ver` 版本号 +
  `cccms:dict:d:{ver}:{typeId}`，TTL 600）。**所有字典写操作**（含批量）与**回收站还原 / 彻底删除**
  均 `bump()` 版本号；**回收站视图（`trashed=1`）直查库不缓存**；Redis 不可用时降级直查库。
- **批量操作**语义同用户模块（只作用于可见行 + 跳过并回报）。

---

## 十一、系统配置（Config）

| 项 | 内容 |
|----|------|
| 功能 | 分组展示的键值配置，可视化控件（input / switch / select / input-number / textarea / radio / password） |
| 表 | `sys_config` |
| 前端 | `pages/cccms/config/index.vue`，组件名 `cccms:config`（两栏表单） |

| 接口 | 权限节点 | 说明 |
|------|----------|------|
| `GET /config` | `cccms:config:index` | 配置列表（可按 `group` 过滤；`password` 项只回 `has_value`） |
| `GET /config/ui` | `#[NoLogin]` | **公开**：登录页 / 前端初始化需要的品牌与 UI 白名单 |
| `POST /config/save` | `cccms:config:save` | 按 name 批量保存（只更新已存在的 name） |

注意事项：

- 读取走 `SysConfig`：Redis（TTL 300s）→ MySQL；保存后 `flush()` 立即失效（**刻意不做进程内缓存**）。
- `type = password` 的项以 `enc:` + `Cipher` 密文存储，前端留空表示不修改。
- 保存 `system.maintenance` 为真时会 `SessionGuard::cut()`，作废非超管旧 token。
- 前端「只提交变更项」，并有未保存离开拦截。
- 新增配置项 = `db/seed.sql` 加一行 + 消费处调用 `SysConfig::getXxx('your.key', 默认值)`。
- 配置项名 i18n：`ConfigLogic::localize()` 按 `config_item.{name}` 覆盖 `title`，并额外下发 `group_label`（分组名翻译）。`group` **保持原值**——它是前端 `?group=` 过滤与分组归并的 key，翻译它会破坏过滤。

种子内置 5 组 32 项（系统 / 界面 / 安全 / 上传 / 日志），详见 [11-常用命令与运维](11-常用命令与运维.md#五系统配置项清单)。

---

## 十二、附件管理（File）

| 项 | 内容 |
|----|------|
| 功能 | 上传、分页列表、分类管理、批量移动到分类、软删除、回收站 |
| 表 | `sys_file`、`sys_category`（`module = 'file'`） |
| 前端 | `pages/cccms/file/index.vue`，组件名 `cccms:file`（树形页） |

| 接口 | 权限节点 |
|------|----------|
| `GET /file` | `cccms:file:index` |
| `POST /file/upload` | `cccms:file:upload` |
| `POST /file/delete` | `cccms:file:delete` |
| `POST /file/move` | `cccms:file:move` |
| `GET /file/category` | `cccms:file:category` |
| `POST /file/category/save` | `cccms:file:category_save` |
| `POST /file/category/update` | `cccms:file:category_update` |
| `POST /file/category/delete` | `cccms:file:category_delete` |

注意事项：

- 模型**参与**数据权限：「仅本人」= `create_by`，「本部门」落到可见部门成员上传。
- URL 不硬编码，统一经 `FileStorage::url()` 生成（前缀来自 `upload.url_prefix`，切 CDN 无需迁移历史数据）。
- 上传安全：扩展名 + 大小上限（默认 10MB）+ **随机重命名**（`Ymd/HiS + 随机 hex`）。
- 去重查询走**带作用域**的查询，否则会把别人上传的附件路径「复用」给你。
- 软删除**刻意不删物理文件**，物理文件在「彻底删除」时才清。
- **在线预览**：出参带 `is_image` / `is_pdf`（按 `upload.image_ext` 与扩展名实时推导）；
  图片用内置预览器，**PDF 用内嵌 `iframe` 对话框预览**，其它类型回退「查看 / 下载」。
- 前端上传走 `el-upload` 直传（原生 XHR），单独登记上传进度条令牌。

更多存储机制见 [11-常用命令与运维](11-常用命令与运维.md#六存储与附件)。

---

## 十三、操作日志（Log）

| 项 | 内容 |
|----|------|
| 功能 | 记录写操作（可选记录读操作）与**登录成功/失败**，支持按路径/结果筛选、查看明细、批量删除；慢接口告警 + 归档到对象存储 |
| 表 | `sys_log`（登录记录固定 `path=/auth/login`，与操作日志同表互斥） |
| 前端 | `pages/cccms/log/index.vue`，组件名 `cccms:log`（列表页 + 明细抽屉） |

| 接口 | 权限节点 |
|------|----------|
| `GET /log` | `cccms:log:index` |
| `GET /log/export` | `cccms:log:export` |
| `GET /log/trace` | `cccms:log:trace` |
| `POST /log/delete` | `cccms:log:delete` |

注意事项：

- 写入侧在 `OperationLog` 中间件：只记 `POST/PUT/DELETE/PATCH`；`log.record_read` 打开后 GET 也记。
- 记录内容：`node`（slug）+ `title`（注解操作名）、请求参数（query + body，body 优先）、执行结果、
  IP、UA、方法、路径、耗时、状态码、**`trace_id`（请求链路 ID）**。
- **链路检索**：响应头 `X-Trace-Id`（失败响应体里也有 `trace_id`）可拿到一次请求的链路 ID，
  在日志页按「链路ID」精确筛选，即可定位该请求的所有记录。
- **链路聚合视图（P2-6）**：`GET /log/trace?trace_id=xxx` 返回该链路**全部记录按 `id` 升序**
  （即请求内时间顺序）+ 汇总 `{total, failed, cost}`。走同一份 `newScopedQuery()`，
  **看不见的日志不出现**（越权行不泄露）；`trace_id` 为空抛 422（避免无条件下捞全表）；
  单次上限 200 条。前端以 `el-timeline` 时间线展示，并支持
  **从 URL 带 `trace_id` 直接进入**（`/log?trace_id=xxx`，便于从报错弹窗跳转）。
- **导出**：最多 10000 行，含链路 ID 列。
- 敏感字段递归脱敏（password 系列、token 系列、secret 等），**深度上限 3**（规则抽到
  `support/LogRedactor.php`，归档冷数据时复用同一份，避免两处漂移）。
- 上传接口 body 为空时会补 `_files`（形如 `file:报告.pdf`）。
- 文本长度上限 8000 字符。
- **日志写入失败绝不影响业务**。
- **慢接口告警**：请求耗时超过 `log.slow_threshold`（毫秒，默认 3000，0 = 关闭）时额外写一条
  告警日志到 `runtime/logs/slow.log`；覆盖**所有**请求（含 GET），与是否记操作日志无关。
- **归档到对象存储**：`php webman cccms:log-archive` 把早于 N 天的记录序列化为 JSONL(gzip)
  上传后删主库（**先存后删**，可重入）；内置任务「日志归档到对象存储」默认**停用**。
  用法见 [11-常用命令与运维](11-常用命令与运维.md#一cli-命令)。
- 模型参与数据权限：删除 / 清空**只影响自己可见范围内的日志**。
- **登录日志已并入本表**：登录成功/失败由 `AuthLogic` 写 `path='/auth/login'` 的记录（登录接口是
  `#[NoLogin]`，中间件拿不到操作人），失败原因记在 `message`，用「请求路径」筛 `/auth/login` +
  「结果」即可定位；登录接口本身不再由 `OperationLog` 中间件重复记录。
- 日志表没有软删列，删除即物理删除；按 `log.keep_days` / `log.auto_clean` 由定时任务清理（需自行配置任务，种子未预置）。

---

## 十四、定时任务（Crontab）

| 项 | 内容 |
|----|------|
| 功能 | Cron 任务增删改查、可视化编辑表达式、可调度任务类下拉、立即执行、执行日志 |
| 表 | `sys_crontab`、`sys_crontab_log` |
| 前端 | `pages/cccms/crontab/index.vue`，组件名 `cccms:crontab` |

| 接口 | 权限节点 |
|------|----------|
| `GET /crontab` | `cccms:crontab:index` |
| `GET /crontab/targets` | `cccms:crontab:targets` |
| `GET /crontab/logs` | `cccms:crontab:logs` |
| `POST /crontab/save` | `cccms:crontab:save` |
| `POST /crontab/update` | `cccms:crontab:update` |
| `POST /crontab/delete` | `cccms:crontab:delete` |
| `POST /crontab/run` | `cccms:crontab:run` |

### 14.1 增强项（重叠 / 超时 / 重试 / 分组）

| 字段 | 说明 |
|------|------|
| `group_name` | 任务分组，仅用于归类与筛选（**不是并发队列**） |
| `overlap` | `skip`（默认）：上次未结束则本次跳过；`allow`：允许并发 |
| `timeout` | 超时秒数，`0` = 不限；超时会**释放运行锁并写一条 `status=3` 日志** |
| `retry_times` / `retry_interval` | 失败后重试次数（0~10）与间隔（秒） |
| `retry_left` / `retry_at` | 运行时字段：剩余重试次数与下次重试时间 |
| `sys_crontab_retry` | **独立的失败重试队列表**：重试到期的任务从这里取出，与正常 cron 判定互不干扰 |
| `running` / `running_at` | 运行时字段：运行锁 |

调度顺序（每 tick）：**① 到期的失败重试（从 `sys_crontab_retry` 取，不受 cron 约束）→ ② 正常 cron 调度**。
重试队列独立成表后，重试不会再「吃掉」该秒的正常调度（原先共用一个判定会跳过 cron）。

> ⚠️ **超时的能力边界**：任务在调度进程**同一个进程内**执行，PHP 无法从外部强杀正在执行的调用。
> 因此「超时」只能做到「解锁 + 留痕 + 让后续调度恢复」，**不能真正中断卡住的代码**。
> 需要硬超时的任务请自行在代码里做分段 / 协作式检查，或改用独立进程执行。

### 14.2 执行日志状态

`sys_crontab_log.status`：`1` 成功 · `0` 失败 · `2` 跳过（上次未结束）· `3` 超时释放；
`source`：`cron` / `retry` / `manual` / `timeout`。
「跳过」日志按任务**每 60 秒最多一条**（避免每秒任务 + 长执行把日志表刷爆）。

### 14.3 其他约定

- 调度进程 `plugin/cccms/process/Crontab` **每秒 tick**；**仅 Linux / macOS 生效**（Windows 无自定义进程）。
- 刷新表达式为**六段**（秒 分 时 日 月 周），与 Linux 五段不同。
- **执行白名单**：只执行实现 `CrontabTask` 接口的类（杜绝 RCE）；`targets` 扫描 `plugin/*/command/task` 下的实现类。
- 日与周同时限定时按 **AND**；不做月份实际天数回绕（31 号在 2 月不触发）。
- 同一秒按 key 去重，避免重复执行。
- 任务在调度进程内**串行**执行，长任务会顺延后续 tick。
- 「立即执行」也走重叠保护：若该任务正在执行中，手工触发会被跳过（返回 `status=2`）；
  手工执行**不改变失败重试状态**。
- 删除为软删，执行日志保留。
- 内置任务：`plugin/cccms/command/task/LogCleanTask`（清理历史操作日志，读取
  `log.keep_days` / `log.auto_clean`）；`seed.sql` 已预置「每天 03:00 执行」的任务。

新增一个任务类：

```php
namespace plugin\cccms\command\task;

use plugin\cccms\support\CrontabTask;

class MyTask implements CrontabTask
{
    public function run(array $params = []): string
    {
        // 业务逻辑
        return '执行完成：处理 10 条';
    }
}
```

然后在「定时任务 → 新增」中从「执行目标」下拉选择它（类名带插件前缀）。

---

## 十五、代码生成器（Generator）

| 项 | 内容 |
|----|------|
| 功能 | 按库表生成 Model / Logic / Controller / 路由 / 前端 API / 前端页面 6 类产物，并登记菜单 |
| 表 | 读 `information_schema`；写文件 + 登记 `sys_menu` |
| 前端 | `pages/cccms/generator/index.vue`，组件名 `cccms:generator` |

| 接口 | 权限节点 |
|------|----------|
| `GET /generator/tables` | `cccms:generator:tables` |
| `GET /generator/columns` | `cccms:generator:columns` |
| `POST /generator/preview` | `cccms:generator:preview` |
| `POST /generator/generate` | `cccms:generator:generate` |

生成的产物路径：

```
server/plugin/{plugin}/app/model/{Module}.php
server/plugin/{plugin}/app/logic/{Module}Logic.php
server/plugin/{plugin}/app/controller/{Module}Controller.php
server/plugin/{plugin}/config/route/{module}.php
frontend/src/api/auto/{plugin}/{module}.ts
frontend/src/pages/{plugin}/{module}/index.vue
```

注意事项：

- 忽略字段：`id` / `create_time` / `update_time` / `delete_time`。
- **模型模板按表结构生成数据权限声明**：
  - 有 `create_by` + `dept_id` → 默认语义；
  - 只有 `create_by` → owner + 自定义 `deptScope()`；
  - 都没有 → `['no_baseline' => true]` + TODO 注释。
- Logic 模板查询走 `{Model}::newScopedQuery()`（有软删列时自动带回收站数据源开关），不再生成 `Db::name()`。
- 生成即符合 fail-closed（Controller 自带注解模板）。
- 文件已存在且未勾选「覆盖」→ 422。
- 生成后会 `registerMenu`（`type = 2`，挂到插件目录节点下），并给出可直接粘贴到 `db/menu.php` 的 `menu_snippet`。

---

## 十六、回收站（Recycle）

| 项 | 内容 |
|----|------|
| 功能 | 对软删除的业务主数据执行「还原」与「彻底删除」 |
| 表 | 跨表操作（注册表见下） |
| 接口 | `POST /recycle/restore`、`POST /recycle/delete`（**无独立列表接口**） |
| 前端 | 不是单独页面：各模块列表页表格右上角的 `RecycleToggle` 切换 |
| 权限节点 | `cccms:recycle:restore`、`cccms:recycle:delete`（`group = cccms:setting`） |

内置类型（`RecycleLogic::TYPES`，共 12 类）：

```
user / role / dept / post / dict_type / dict_data / category / crontab / data_rule / file / menu / notice
```

业务插件可在 `plugin/{插件}/db/recycle.php` 声明额外类型，会被自动合并。

注意事项：

- **列表数据源切换**：列表接口带 `trashed=1`，而不是单独的回收站接口。
- 回收站视图 = `Model::onlyTrashed()`。
- 恢复前校验：
  1. 唯一键表（`user.username` / `role.code` / `post.code` / `dict_type.type` / `menu.node`）的唯一值是否**被占用**（已排除待恢复行自己）；
  2. 上级是否仍在回收站（`dept` / `category` / `dict_data` / `menu`，防止恢复出孤儿节点）。
- 彻底删除：`file` 先删物理文件，`menu` 先清 `role_node` 授权，再真删。
- 要操作已删行**必须 `withTrashed()`**，否则静默 0 行。

前端使用顺序（`useRecycle` 必须在 `useTable` 之前）：

```ts
const { recycle, toggle, onRestore, onForceDelete } = useRecycle('user', { reload, ids, clear })
// 1) 列表 api 注入 trashed: recycle.value ? 1 : 0
// 2) <ArtTable :recycle="recycle" @restore="onRestore" @force-delete="onForceDelete" />
// 3) <RecycleToggle :active="recycle" :label="t('user.recycleLabel')" @toggle="toggle" />
```

---

## 十七、系统维护（Maintenance）

| 项 | 内容 |
|----|------|
| 功能 | 顶栏「系统同步 / 清理缓存」，等价于 `menu-sync` + `perm-scan` + 清缓存 |
| 接口 | `POST /system/refresh`（范围：`all` / `menu` / `perm` / `schema` / `cache`） |
| 权限节点 | `cccms:config:refresh`（挂在「配置管理」菜单下） |
| 前端 | `layouts/components/LayoutHeader.vue` 的下拉入口 |

各范围行为：

| scope | 行为 |
|-------|------|
| `menu` | `MenuSyncer::syncAll()` |
| `perm` | `PermScanner` 扫描 + 校验（**有错整批不写库**） |
| `schema` | 执行各插件 `db/schema.sql` |
| `cache` | `SysConfig::flush()` + `PermissionCache::bump()` + `PermissionMeta::flush()` |
| `all` | 以上全部 |

> `cache` 里的 `PermissionCache::bump()` 会让**所有用户**的角色 / 权限缓存立即失效（版本号 +1），
> 是「改了权限但界面没生效」时最快的手工兜底手段。

与 CLI **共用同一份实现**，避免结果不一致。同步完前端会自动重挂动态路由并刷新当前用户权限。

---

## 十八、前台应用（`plugin/index`）

| 项 | 内容 |
|----|------|
| 功能 | 公开前台接口骨架，与后台中间件隔离 |
| 中间件 | 仅 `Cors`（**没有** `CheckLogin` / `CheckAuth`） |
| 路由 | `GET /site/ping`、`GET /site/home` |
| 控制器 | `HomeController`（继承 `BaseController`，复用统一信封） |

| 接口 | 返回 |
|------|------|
| `GET /site/ping` | `{app: 'index', time}` |
| `GET /site/home` | `{title: 'CCCMS 前台', notice: '前台插件骨架，按需在此扩展业务'}` |

注意事项：

- 该插件**无 `db/menu.php`**，不参与权限注解校验、菜单同步与数据权限检查。
- 新增前台接口时照旧声明路由即可，不需要 `#[NoLogin]`（插件级中间件已隔离）。

---

## 十九、在线用户（Online）

| 项 | 内容 |
|----|------|
| 功能 | 查看当前在线会话（用户、设备类型、操作系统、浏览器、IP、登录时间、最后活跃、会话过期）；按**会话**或按**用户**强制下线 |
| 存储 | Redis（`support/OnlineSession.php`），不落库。设备类型 / 系统 / 浏览器由 UA 在读取时派生，不单独落库 |
| 前端 | `pages/cccms/online/index.vue`，组件名 `cccms:online` |

| 接口 | 权限节点 |
|------|------|
| `GET /online` | `cccms:online:index` |
| `POST /online/kick` | `cccms:online:kick` |
| `POST /online/kickUser` | `cccms:online:kick_user` |

实现要点：

- 会话索引：`cccms:online:s:{jti}`（Hash，会话明细）+ `cccms:online:index`（ZSet，按活跃度排序）。
- 登录时 `register()`；`CheckLogin` 每次请求 `touch()` 刷新活跃时间（**60 秒节流**，避免每请求写 Redis）；
  登出时 `remove()`。
- 索引 ZSet **不设 TTL**（比单条会话活得久），过期成员由列表查询与 `prune()` 惰性清理。
- **下线必须在两处同时生效**：`OnlineSession::remove*()`（从列表移除）+ `TokenBlacklist`
  （作废已发出的令牌）。只做前者属于「假下线」—— 对方手里的 token 依然能用。
- 不允许下线自己（按会话 / 按用户都会校验），否则操作者会被立刻踢出去。
- **量级**：列表为**服务端分页 + 服务端过滤**，索引 ZSet 按 `SCAN_BATCH = 200` 分段扫描
  （不再有「单次最多 2000 条」的内存过滤上限），在线数上万也可用。
- **黑名单 TTL 精确化**：会话 Hash 里存了令牌 `exp`，`kick()` 按真实剩余有效期写黑名单，
  不会因为 `security.token_ttl` 调大而长期驻留。
- **滑动续期会迁移会话**：令牌续期后 `jti` 变了（见 [05-鉴权与权限 §5.2](05-鉴权与权限.md#52-tokenservicejwt)），
  `OnlineSession::rotate()` 把会话迁到新 `jti` —— 不迁移的话，这里踢掉的是已经废弃的旧令牌，
  对方手里的新令牌依然有效，形成「假下线」。

---

## 二十、通知公告（Notice）

| 项 | 内容 |
|----|------|
| 功能 | 管理端发布通知 / 公告，**支持按部门 / 角色 / 指定用户定向投放**；所有登录用户可在顶栏「我的消息」查看，带未读角标 |
| 表 | `sys_notice`、`sys_notice_target`（投放范围）、`sys_notice_read` |
| 前端 | 管理页 `pages/cccms/notice/index.vue`（组件名 `cccms:notice`，含发布范围选择器 + 「回执」抽屉）；阅读入口在顶栏铃铛抽屉 |

**管理侧接口**

| 接口 | 权限节点 |
|------|----------|
| `GET /notice` | `cccms:notice:index` |
| `GET /notice/read` | `cccms:notice:read` |
| `GET /notice/options` | `cccms:notice:options`（投放候选：部门 / 角色 / 用户） |
| `GET /notice/report` | `cccms:notice:report` |
| `GET /notice/users` | `cccms:notice:users` |
| `POST /notice/save` | `cccms:notice:save` |
| `POST /notice/update` | `cccms:notice:update` |
| `POST /notice/delete` | `cccms:notice:delete` |

**阅读侧接口（登录即可，只返回自己的数据）**

| 接口 | 说明 |
|------|------|
| `GET /notice/my` | 我的消息分页（已发布 + 未过期），带 `is_read` |
| `GET /notice/unread` | 未读数量（顶栏角标） |
| `POST /notice/markRead` | 单条标记已读 |
| `POST /notice/markAllRead` | 全部标记已读 |

注意事项：

- 字段：`type`（1 通知 / 2 公告）、`level`（1 普通 / 2 重要）、`status`（1 发布 / 0 草稿）、
  `publish_at`、`expire_at`（为空则不过期）。
- 已发布但未填 `publish_at` → 视为「立即发布」。
- 「已读人数」是 `read_count` 冗余计数，**只在首次已读时 +1**。
- 模型声明**不参与数据权限**：公告是广播内容，没有「归属人」语义。
- 支持回收站：`RecycleLogic` 已登记 `notice` 类型（软删时同时清理已读记录）。
- 阅读侧为 `#[NoAuth]`（登录即可）是恰当的：它只返回**当前用户自己的**数据，不涉及全量查询。
- 定向投放范围由 `sys_notice_target` 落库（部门 / 角色 / 指定用户三类），候选数据由
  `GET /notice/options` 提供，已读回执由 `GET /notice/report` 提供。

---

## 二十一、租户管理（Tenant）

| 项 | 内容 |
|----|------|
| 功能 | 单库共享表 + `tenant_id` 的租户隔离；超管**显式切换**租户；租户之间数据互相不可见 |
| 表 | `sys_tenant`（只存真实租户 `id >= 1`；平台租户 `0` 是虚拟租户，**不落库**）+ 12 张隔离表的 `tenant_id` |
| 前端 | `pages/cccms/tenant/index.vue`；顶栏切换器在 `layouts/components/LayoutHeader.vue`（**仅超管可见**） |

| 方法 | 接口 | 权限节点 | 说明 |
|------|------|----------|------|
| `index` | `GET /tenant` | `cccms:tenant:index` | 租户列表（平台租户行由 Logic 虚拟补出） |
| `read` | `GET /tenant/read` | `cccms:tenant:read` | 租户详情 |
| `save` | `POST /tenant/save` | `cccms:tenant:save` | 新增；`code` **全局唯一** |
| `update` | `POST /tenant/update` | `cccms:tenant:update` | 修改；**平台租户不可改** |
| `delete` | `POST /tenant/delete` | `cccms:tenant:delete` | 删除；**平台租户不可删**，仍有账号（含回收站账号）占用时拒绝 |

注意事项：

- 隔离机制、`withoutGlobalScope()` / `withoutAllScopes()` 的差异、直查必须走 `TenantContext::table()`
  等口径见 [06-数据权限 §十三](06-数据权限.md#十三多租户租户是硬边界档位是租户内的软范围)。
- 五个接口都在 `TenantLogic::assertPlatformAdmin()` 处二次校验：**必须是超管，且当前生效租户 = 平台租户**
  —— 超管切进某租户后就失去了租户管理入口，避免「在租户里改租户」。
- 授权时**不要**把 `cccms:tenant*` 下发给租户内角色。
- 新增业务表若要参与隔离：加 `tenant_id` + `idx_tenant`、模型声明 `$tenantScope = true`、
  并登记进 `TenantContext::TABLES`（供直查路径使用）；全局唯一键**不要**加租户列。

---

## 二十二、模块与权限节点总览

| 模块 | 权限节点数 | 备注 |
|------|-----------|------|
| config | 3 | `index` / `save` / `refresh` |
| crontab | 7 | 含 `targets` / `run` / `logs` |
| data_rule | 13 | 含受控表 4 个（`table_*`） |
| dept | 4 | |
| dict | 16 | 字典 12（含 4 个 `batch_*`）+ 分类 4 |
| file | 8 | |
| generator | 4 | |
| log | 4 | 含 `export` / `trace` |
| menu | 4 | |
| notice | 8 | 含 `options` / `report` / `users` |
| online | 3 | `index` / `kick` / `kick_user` |
| post | 6 | 含 2 个 `batch_*` |
| recycle | 2 | `group = cccms:setting` |
| role | 7 | 含 `copy` |
| tenant | 5 | `index` / `read` / `save` / `update` / `delete`；**平台级**，不下发给租户内角色 |
| upgrade | 5 | 系统升级（`index` / `check` / `tags` / `init` / `run`） |
| user | 12 | 含 `reset_password` / `export` / `template` / `import` / 3 个 `batch_*` |
| **合计** | **111** | 由 `perm-scan` 自动生成，无需手工维护 |

> 数字以 `php webman cccms:perm-scan` 的实际输出为准；新增接口后节点会同步增加。
> 上表为从 `sys_menu`（`type = 3` 的按钮节点）按模块聚合所得，`login_log` 模块已并入 `log`，不再单列。
