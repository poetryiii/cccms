/**
 * 静态路由标题。
 *
 * 只放**前端静态声明**的路由（登录、工作台、个人中心、错误页）。
 * 动态路由的标题来自后端菜单树，已由后端按当前请求语言下发（见 `menu.php`），
 * 前端不重复维护 —— 因此这里没有 `menu` 命名空间。
 */
export default {
  login: '登录',
  dashboard: '工作台',
  profile: '个人中心',
  forbidden: '无权访问',
  notFound: '页面不存在',
  /** 未匹配到路由时的兜底标题 */
  unknown: '未命名页面',
}
